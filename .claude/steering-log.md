# Steering Log — carenote-ai ハーネス改善記録

問題が複数回発生したらここに記録し、ガイド／センサー／テストのいずれかで対策する。

## Template
```
### YYYY-MM-DD: [問題の要約]
- **発生回数**: N回
- **問題**: 何が起きたか
- **対策**: ガイド/センサー/テストのどれを追加したか
- **ファイル**: 変更したファイルパス
- **教訓**: 学んだこと
```

## Log

### 2026-05-13: cortex-lite ハーネス導入
- **発生回数**: 1回（初期セットアップ）
- **問題**: ユーザー層 `~/.claude/CLAUDE.md` を整備したのを機に、carenote-ai 側もハーネス化する必要があった
- **対策**: ガイド + センサー + Auto Review の3要素を一括導入
- **ファイル**: `CLAUDE.md`, `.claude/settings.json`, `.claude/hooks/*.sh`, `.github/workflows/quality-gates.yml`
- **教訓**: 新規プロジェクトに `/harness-init` を流せばこのセットが自動配置されるようにする

### 2026-06-09: biome.json が v1 書式のまま放置され Quality Gate が機能停止していた
- **発生回数**: 1回（製品再定義の P0 資産整理で発覚）
- **問題**: 前セッションで追加した `biome.json` が Biome v1 書式（`files.ignore` / トップレベル `organizeImports`）のまま未コミットで残り、インストール済みの Biome v2.4.10 では**設定エラーで check 自体が動かない**状態だった。つまり Quality Gate（②）が見かけ上あるのに無効化されていた。
- **対策（センサー復旧）**: 公式 `npx biome migrate --write` で v2 書式へ移行（`files.includes` の否定glob・`assist.actions.source.organizeImports`）。その後 `biome check` が初めてコードに対して走り、既存の評価UIに 37 errors を検出 → 自動修正＋手当てで緑化。
- **ファイル**: `biome.json`, `components/*`, `lib/supabase/*`, `app/(dashboard)/evaluate/page.tsx`
- **教訓**: 設定ファイルを**追加した時点で実際に走らせて緑を確認**する（"配置した≠機能している"）。Observe before Act（§2.6-C）。導入PRで `npm run check` を1回通すことを必須に。

### 2026-06-09: 既存レガシUIの a11y/style 指摘で baseline を緑化する際の severity 判断
- **発生回数**: 1回（#上と同じ P0 で連鎖）
- **問題**: Biome 初回稼働で、既存の評価UIに `noArrayIndexKey`（静的な文字列リストの index key）と `useSemanticElements`（div+role=button より `<button>` 推奨）が error として多数検出。後者はドロップゾーンが内部に `<input type=file>` を持つため `<button>` 化が HTML 的に不正で、機械的修正が不適切だった。
- **対策（severity 調整）**: 実害の大きい指摘（button type 欠落・SVGのa11y・非null assertion・キーボード操作の欠如）は**正しく修正**。一方 `noArrayIndexKey` / `useSemanticElements` は**rule severity を warn に降格**して追跡負債化（inline `biome-ignore` ではなく rule 単位で調整＝ §2.1 disable禁止の精神に沿う）。
- **ファイル**: `biome.json`
- **教訓**: 過去の steering #4/#5 と同根 ── **禁止の強さはプロジェクト成熟度に比例**させ、既存負債で無関係作業（製品再定義の P1）を止めない。warn は「見えるが止めない」追跡手段。レガシ評価UIを作り直す際に error へ再昇格する。

### 2026-07-08: 危険 Bash ガードの所有権分割 ── pre-tool-guard.sh を撤去

- 汎用ガード（force push・reset --hard/clean -f・supabase db reset・.env の git add）はグローバル層 `~/.claude/hooks/pre-bash-guard.py` が ask/block で所有するため、プロジェクト側の複製（旧式・`-f` 短縮形を素通しするドリフト版）を撤去し settings.json の登録も削除。固有分が残らないためフック自体を廃止し、再ドリフト検知の空打ちテスト `test_project_hooks.sh` を新設（7/7 PASS）。正本= HARNESS-RUNBOOK §5・決定= decisions-log 2026-07-08。
- **横展開（maoucastle-game PR#5 独立審査 Critical への対処）**: グローバル層未導入環境（CIランナー・新規マシン）では撤去したガードが完全素通しになるため、「フック全撤去」から「fail-safe 専用フックに縮退」へ変更 ── 存在チェックで所有者がいる環境では休眠し、不在時のみ旧4ガード相当（force push の -f 素通し穴は修正済）で block。settings.json に再登録。空打ちテスト16/16 PASS。あわせて main 由来の .claude/launch.json biome フォーマット違反（quality-gate を赤にしていた既存違反）を同PRで修正。

## 2026-09-11 画面（"use client"）がサーバー専用モジュールを値 import してビルド断
- 事象: `app/(dashboard)/rescue/page.tsx` が `lib/generation/rescueIntake.ts` から定数（種別一覧）を import → 同ファイルが Anthropic SDK を読むため `node:fs` 等がブラウザ向けビルドに混ざり `Module build failed`。tsc・vitest は通るので build まで回さないと気づけない。
- 対策: 型と定数を `lib/generation/intakeTypes.ts`（純粋）へ分離し、画面はそちらを読む。rescueIntake.ts は `export *` で再輸出。
- 学び: 画面から `lib/generation/*` を **値 import する時は純粋モジュールか確認**（`import type` なら消えるので安全）。完了報告前の `npm run build` は省略しない（§2.8-B）。

## 2026-09-11 同名系ヘルパーの取り違え（getValue(draft,key) を欄の読み取りに使い、追記が上書きになる欠陥）
- 事象: 追記モード（previewAppend/applyAppend）と転記シート（injectKaipokeSheet）で `getValue(el)` と書いたが、既存の `getValue(draft, key)` は下書きから値を取る関数。要素を渡すと常に "" → 「既存は空」と誤判定し、**過去の記録を消さないための機能が上書きになる**欠陥。テストは DOM を通らないので緑のまま。コミット前の読み返しで発見。
- 対策: `readFieldValue(el)` を新設して両所を差し替え。テストで「getValue に要素を渡すと空」を明示。
- 学び: ①DOM を触る関数は名前に `Field`/`El` を入れて下書き系と区別する ②「消さない」系の安全機能は、実機（またはjsdom）で **既存文章が残ること**を必ず確認する（次回: jsdom テストを1本足す）。

## 2026-09-12 独立審査（graph-doubt）が拾った「自分では見えない」欠陥 ── 4つの型
- **事象**: main 取り込み前の敵対検証で critical 8件。自分のセッションのセルフレビューでは1件も見えていなかった。
  1. **見えない文字**: `vault.ts` に生の NUL を書き、git がバイナリ扱い→差分が PR にも triage にも出ない。Write ツールは `\uXXXX` を実文字に展開することがある（本セッションでゼロ幅文字でも再発）。
  2. **fail-closed の例外が広すぎる**: 関係者名簿だけ「表が無くても動くように」と warn で続行＝家族名が消えないまま AI へ。安全網に「便利な例外」を作った。
  3. **戻した値の再送**: 二枚方式で実値に戻した下書きを、後段 API が「記号版のはず」と信じてそのまま AI へ。**設計変更（restoreDeep 導入）が、前提の古い呼び出し側を壊す**型。
  4. **テストが呼び出し側を守っていない**: 純粋関数は緑でも、ルート層（SSRF・再黒塗り・vault 復元）は1行消しても緑。
- **対策**: ①センサー `tools/check-invisible.mjs`（CI＋Stop フック） ②関係者表も 503（テストで固定） ③規約「戻した帳票を再び AI へ送る API は maskDeep を通す」を CONTEXT-MAP へ ④ルート層テスト4本＋偽 Supabase テスト＋jsdom DOM テスト。
- **学び**: (a) 慎重領域は「自分で合格判定しない」（§2.7-E）が実効性を持った初回。graph-doubt 30 エージェント・約 24 分・8件確定。(b) 「既存も同じだから安全」と思った箇所（Blob 公開ストア）が一番重い指摘だった。(c) Write ツールでエスケープ列を書く時は、書いた直後に `node tools/check-invisible.mjs` を回す。(d) `git stash` / `pop` は autocrlf=true の環境で作業ファイルを CRLF に戻し biome を赤にする ── 使った後は `sed -i 's/\r$//'` で戻す。

## 2026-09-12 CI の自動審査（claude-triage review-pr）が「成功」のまま何も審査していなかった
- **事象**: PR #9 の review-pr が 9 秒で success。ログに `Unexpected input(s) 'prompt'` の警告があり、`@beta` 版のアクションは `prompt` 入力を受け付けず、Claude の実行ステップが全部 skipped。独立審査 critical #2「PR＋triage が一度も走った証拠がない」の実体。
- **対策**: MaouCastle 本体（2026-09-06 に実 PR で疎通済み）と同じ `@v1`＋`claude_args`（--max-turns 40）＋`timeout-minutes: 20` へ揃える。triage-issue 側も同じ穴なので同時に修正（横展開）。
- **学び**: 「CI が緑」は「検査が走った」と同義ではない。所要秒数が異常に短い job は疑う（Observe before Act §2.6-C）。センサー候補: review-pr の成功条件に「レビュー投稿の有無」を加える。

## 2026-09-12 移行が要る変更を「実装したから有効」と書いた ── PR #14（名簿の事業所共有）独立審査
- **事象**: 名簿の読み出し範囲を `created_by` から「事業所（org_id）＋組織加入前の自分」に広げたが、
  1. **既存データに届かない**: 本番の `clients` は全件 `org_id = null`。組織を有効にしても**同僚には見えない**まま。
     SQL には索引だけ入れて `update ... set org_id = ...`（移行）を書いていなかったので、Issue #13 は実質未解決。
  2. **採番が件数ベース**: 「事業所の行」と「自分の org_id=null 行」が混ざる範囲を `count` で数えて `A,B,...` を振ると、
     別人が同じ `A` を取りうる。部分一意索引（`where org_id is not null`）はこの組合せを止めない。
     記号が重なると `restoreNames` が**他人の実名**に戻す（審査側が実行で再現）。
  3. **断定**: マニュアル・DATA-HANDLING に「組織に所属していれば全員分に効く」と書いた（移行前は成り立たない）。
- **対策（同 PR）**: ①採番を「範囲内の最大＋1」へ（`clientCodeIndex` を新設・往復テスト800件） ②`assertCodesUnique`
  で「同じ記号に違う氏名」を見つけたら送信中止（移行の適用有無に依存しない安全網） ③名簿の読み出しに件数上限
  （900件）を付け、超えたら中止（PostgREST は既定1000行で**黙って**打ち切る＝fail-open だった） ④壊れた orgId は
  黙って本人スコープへ落とさず例外 ⑤実名の保存に失敗したら利用者ごと取り消す ⑥`supabase/client_org_scope.sql` に
  移行手順（手順2の重複チェック→手順3の backfill→手順4の確認）を同梱 ⑦ルート9本の範囲受け渡しを
  `tests/api/orgScope.route.test.ts` で縛る（2本を壊して2件落ちることを実測）。
- **学び**: (a) **スキーマの意味を変える変更は「新規データの経路」と「既存データの移行」を必ず対で出す**
  ── 実装だけでは既存利用者に1ミリも届かない。(b) 「範囲」を広げたら**採番・一意制約も同じ範囲**に合わせる
  （片方だけ広げると衝突する）。(c) DB クライアントの**既定の行数上限は黙って効く** ── 安全に関わる読み出しには
  明示の limit と超過時の中止を付ける。(d) ドキュメントの「〜に効きます」は前提条件（管理者の作業）とセットで書く。
- **補足（審査の誤検知）**: 審査は「`lib/db/clients.test.ts` が同一コミットのまま 20 回中 6 回失敗する」と報告したが、
  ローカルで 12 回連続実行して 12 回とも 12 passed。並列エージェントが同じ作業ツリーを書き換えていたための汚染と判断。
  **審査の指摘も一次情報で確かめてから直す**（§2.6-A は審査結果にも適用する）。

## 2026-09-13 独立審査を「作業中の作業ツリー」で回すと、審査と作業が互いを壊す
- **事象**: graph-doubt（並列エージェント）を carenote-ai の実ディレクトリに対して起動したまま、
  同じディレクトリで `npx tsc` / `npx vitest` を実行したら
  `This is not the tsc command you are looking for` で落ちた。調べると `node_modules/.bin` が空。
  審査エージェントが検証のため `npm ci` / `npm install` を走らせ、その最中は `.bin` が一時的に消える。
  **前回（2026-09-12）の審査が「テストが20回中6回失敗する」と報告したのも同じ原因**
  （ローカルで12回連続実行して 12/12 通過・再現せず）。審査側の環境汚染を「実装の不安定」と読み違えていた。
- **対策**: ①審査を回している間は同じツリーで npm 系コマンドを実行しない（docs 編集・git は可）
  ②次回から審査エージェントには `isolation: 'worktree'`（各エージェントに git worktree を与える）を検討する
  ③審査プロンプトに「作業ツリーを書き換えないこと」と書くだけでは不十分だった
  ── `npm ci` は「書き換え」と認識されにくいので、隔離で構造的に防ぐ。
- **学び**: **審査の指摘も一次情報で確かめる**（§2.6-A は審査結果にも適用）。
  とくに「不安定」「たまに失敗する」系の指摘は、審査環境そのものを疑う。

## 2026-09-13 「実装した」と「効いている」の距離 ── PR #14 第2回 独立審査で確定した critical 群
- **事象**: 名簿の事業所共有について、実装・テスト・移行SQL・文書まで揃えて出したが、審査は不合格。
  型の違う穴が同時に出た:
  1. **鍵になる前提が外部設定に依存し、アプリからは触れも見えもしない** ── Clerk の `orgId` は
     「所属」ではなく「いま選んでいる事業所（Active Organization）」。アプリに組織を選ぶ導線が1つも無く
     （`OrganizationSwitcher`/`setActive`/`useOrganization` が 0 件）、null でも黙って本人スコープへ縮退する。
     それなのに顧客配布物（説明書・マニュアル・PDF）は「所属していれば共有されます」と断定していた。
  2. **親子データを別々の条件で引くと、片方だけ落ちても誰も気づかない** ── 利用者・氏名・関係者の3表を
     それぞれ `org_id` で絞っていたため、移行が3表そろわない／組織未選択で登録された関係者がいると
     「利用者は見えるのに氏名だけ名簿から落ちる」。落ちた名前は置換も漏れ検査も効かず素通り。
  3. **安全網が「見えている範囲」しか見ていない** ── `assertCodesUnique` は復号できた氏名だけを突き合わせ、
     `expandAliasVariants` は同じ表記の2人目を**黙って捨てて**いた（夫婦・同姓同名・二重登録で実在）。
     捨てられた人は名簿に載らず、本文の「A様」を戻すと別人の氏名になる。
  4. **待っても直らない失敗に「少し待ってからもう一度」と言っていた**。
  5. **生成物が仕様に適合していなかった** ── 字幕 .vtt の `NOTE` 直後に空行が無く、ffmpeg では字幕 0 件。
     手順書には「残しても再生に影響はない」と誤った断定。
  6. **テストが緑でも走っていなかった** ── jsdom のテストファイルがワーカー起動タイムアウトで落ちても、
     vitest は `Errors 1 error` と出しながら**終了コード 0** を返す。CI もローカルも合格と判定していた。
- **対策（同PR）**: ①`components/SharingStatus.tsx` で共有状態を常時表示＋その場で切替、`orgId` が null なら warn
  ②氏名2表を**親の利用者IDで引く**（`selectByClientIds`）③重複記号は**復号前**に検出、表記衝突は
  `AliasConflictError` で停止 ④`PermanentAliasError` を分け `ALIAS_PERMANENT_MESSAGE` で返す
  ⑤`buildVtt` に空行を入れ、ffmpeg で 13/14 件パースされることを実測＋テストで固定
  ⑥`tools/run-tests.mjs`（ディスク上の .test.ts 数と実際に走った数を突き合わせ、`Errors` 行があれば失敗）を
  `npm test` に据える ⑦ルート8本に `resolveScope` を入れ、壊れた id でも 503 の JSON で止める。
- **学び**:
  (a) **外部サービスの設定に依存する機能は、「効いているか」を画面に出す**。出せない前提は、文書で
      断定してはいけない（「設定すれば効く」は、設定できる導線があって初めて言える）。
  (b) **親子データは親を唯一の境界にする**。子を親と別の条件で引くと、不整合が必ず fail-open になる。
  (c) **安全網は「捨てる」実装と相性が悪い**。黙って捨てた瞬間、検査対象から消える。捨てるなら止める。
  (d) **終了コード 0 は「全部走った」を意味しない**。テスト本数・ファイル本数まで突き合わせて初めて証拠になる。
  (e) 生成物（字幕・PDF）は**外部のパーサで1回は通す**。人が見て正しそうでも機械が読めないことがある。

## 2026-09-13 第3回 独立審査 ── 「修正そのものが新しい穴になる」2件
- **事象**: 第2回の critical を直した結果が、そのまま新しい critical を生んだ。
  1. **センサーが誤報を出して CI を止めた** ── 「テストが緑でも走っていない」を検出するために
     `tools/run-tests.mjs` を入れたが、GitHub Actions は `CI=true` なので vitest（tinyrainbow）が
     集計行を ANSI エスケープで包む。素の正規表現が当たらず、**40ファイル322テスト全合格なのに exit 1**。
     しかも build ステップが未実行のまま終わっていた。ローカルは色が付かないので緑だった。
  2. **直した表示がスマホに届いていなかった** ── 共有状態の表示を左メニューに置いたが、左メニューは
     768px 未満で消える。にもかかわらず、マニュアル・説明書・管理者手順の7か所に
     「スマートフォンでは画面の上に常に出ています」と**自分で書いた**。配布 PDF にも載っていた。
  3. 追加で判明: 安全網3段が生の文字列で比べていたのに、実際の置換（nameRegex）は旧字体・空白を
     畳み込む。「髙橋一郎（A様）」と「高橋一郎（B様）」がすり抜け、別人の実名が帳票に入る。
  4. 追加で判明: その衝突検知が**実在の正常データで誤発火**する ── 主治医や担当ケアマネを複数の
     利用者に登録すると「同じ実名・違う記号」になり、事業所全体の生成が恒久停止するところだった。
- **対策**: ①spawnSync に `NO_COLOR=1` ＋ 照合前に ANSI 除去（二重）。`Errors` 検査を引数の有無に
  かかわらず必ず実行。vitest の include と数え方を `.test.ts`/`.test.tsx` で揃える
  ②`SharingStatus` に compact 版を足し、スマホの本文先頭に常時表示（CSS は 767px 以下に閉じる
  ── 隣接セレクタは display:none でも効くため）③衝突判定を `matchKey`（置換と同じものさし）に統一
  ④「記号が違っても実名が同じなら正常」を明示し、主治医・きょうだいの母のケースをテストで固定。
- **学び**:
  (a) **センサーを足したら、そのセンサーが動く環境で1回は動かす**。ローカルの緑は CI の緑ではない
      （色・TTY・環境変数で出力が変わる）。文字列パースに頼るなら、実環境の生バイト列で確かめる。
  (b) **「〜に出ています」と書く前に、その画面幅で実際に出るか確かめる**。レスポンシブで消える
      場所に置いた表示は、書いた本人が一番信じてしまう。
  (c) **安全網の「同じ」の定義は、本体の「同じ」と必ず揃える**。判定が厳しすぎると素通りし、
      緩すぎると正常データで全停止する。両方向を実データの形でテストする。
  (d) fail-closed を足すときは「**実在する正常な運用で発火しないか**」を必ず一度問う。
      止める仕組みは、止まった時の復旧手段とセットでなければ現場では使えない。

### 2026-09-16: 動画の字幕が二重に出ていた（出どころが2か所）
- **発生回数**: 1回（吉本さんが実機で発見。§2.7-D により1回目で根治）
- **問題**: 字幕を **動画への焼き込み**（`tools/make-video.mjs`）と **再生ページの track タグ**
  （`app/(dashboard)/guide/page.tsx`）の**両方**から出していたため、同じ文が上下に重なって表示された。
  どちらも単体では正しい実装で、片方ずつ見ている限り気づけない。台本→字幕のズレは
  `videoScript.test.ts` で固定していたが、「**出どころが何か所あるか**」は誰も見ていなかった。
- **対策**:
  ①焼き込みに一本化し、再生ページの track タグを削除（mp4 を配っても字幕が付いてくる方を残す
  ── Windows の標準プレーヤーは横に置いた .vtt を読まないことがある）
  ②再発防止テスト（`videoScript.test.ts` の「字幕の出どころは1か所だけ」）。track タグを戻すと落ちる
  ことを実際に確かめた（足す→赤、戻す→緑）
  ③`useMediaCaption` の警告は理由つきで1行だけ抑止（焼き込み済みであることは lint からは見えない）
  ④`docs/MANUAL-VIDEO-SPEC.md` §7・チェックリスト・`docs/CONTEXT-MAP.md` に方針を明記
- **ファイル**: `app/(dashboard)/guide/page.tsx` / `lib/manual/videoScript.test.ts` /
  `docs/MANUAL-VIDEO-SPEC.md` / `docs/CONTEXT-MAP.md`
- **教訓**:
  (a) **同じものを2か所から出していないか**を、作った直後に一度問う。品質ゲートは
      「間違っているか」は見るが「二重になっているか」は見ない。
  (b) 再発防止テストは「**足したら落ちる**」ことまで確かめて初めてセンサーになる。
      通ることだけ見て置くと、実は何も見ていない検査になり得る。
  (c) 出来上がった動画を**自分で1コマ抜き出して見る**だけで気づけた。生成物は数字（長さ・件数）
      だけで確認せず、最後に人の目で見る形にして確かめる。

### 2026-09-16: 合成音声の読み間違いを、作った本人が確かめていなかった
- **発生回数**: 1回（吉本さんが耳で発見。§2.7-D により1回目で根治）
- **問題**: ナレーション91本のうち**50本**で台本と違う音が出ていた。「下書き」→『もとがき』、
  「主治医意見書」→『しゅうぎいんけんしょ』、「CareNote」→『ケールノート』、「六つ」→『むつう』、
  「続柄」→『ぞくがら』、「ご家族」→『ごいえぞく』など。長さも件数も正常で、
  品質ゲートは全て緑だった ── **音は誰も聞いていなかった**。
  さらに調べると、合成は毎回わずかに違う読み方をする（同じ1文が「主事意見書」と読めた回と
  「衆議院憲書」と読めた回があった）。**1回作って終わりでは運任せ**だった。
- **対策**:
  ①センサー `tools/check-reading.mjs` ── 出来た音声を文字起こしに掛け直し、台本と突き合わせて
  音の違う所を機械に見つけさせる（人の耳の代わり）
  ②ガイド `lib/manual/readingDict.ts` ── 読みを固定する辞書48語。MiniMax の `pronunciation_dict`
  へ渡す。`language_boost: "Japanese"` も併用（製品名がこれだけで直った）
  ③テスト `lib/manual/readingDict.test.ts` ── 辞書は**合成前に文字列を置き換える**ので、
  短い語を入れると別の語を壊す（「表/ひょう」は「表示」を壊す）。台本に無い語・食い込む語・
  読みに漢字が混じる語を落とす
  ④`tools/retake-narration.mjs` ── 駄目だった場面だけ読み直させ、文字起こしが台本に一番近い回を
  残す（人が何テイクか録って良いものを使うのと同じ）
- **ファイル**: `tools/check-reading.mjs` / `tools/retake-narration.mjs` / `lib/manual/readingDict.ts` /
  `lib/manual/readingDict.test.ts` / `tools/make-narration.mjs` / `docs/MANUAL-VIDEO-SPEC.md`
- **教訓**:
  (a) **出来上がったものを、出来上がった形で確かめる**。動画なら見る、音声なら聞く。
      長さ・件数・終了コードが揃っていても、中身が正しいことにはならない。
  (b) 自分に無い感覚（耳・目）は、**別の機械に代わりに使わせる**。音声→文字起こし→台本と照合は、
      耳を持たない者が音を検品する唯一の方法だった。最初からこれを回していれば防げた。
  (c) **生成AIの出力は毎回違う**。1回の成功は再現性の保証にならない。品質が要るものは
      複数回試して選ぶか、選んだ結果を機械で確かめる仕組みを付ける。
  (d) 「同じ音の漢字違い（氏名→使命）」と「本当の読み間違い」を区別しないと、直すべき所を見失う。
      検出は機械、判定は意味の分かる者、という分担にした。

### 2026-09-17: 「安全網を強くする変更」が、安全網を弱くしていた
- **発生回数**: 1回（独立審査が検出。§2.7-D により1回目で根治）
- **問題**: 録音を3帳票へ広げるにあたり、送信前の確認画面（黒塗りで消せない名前を人が止める
  **唯一の関門**）を長文に耐える形へ作り直した。ところが「長い欄は先頭400字だけ出す」という
  畳み方にしたため、**長文の既定状態で赤い印（`<mark>`）が1つも出なくなっていた**。
  隠してよいのは読み飛ばせる地の文なのに、確かめるべき赤い言葉の方を隠していた。
  しかも tsc・ESLint・テスト362件・ビルドすべて緑で、仕様書には「実装済・受け入れ条件すべて [x]」
  と書いてあった。**壊れているのに、どのゲートも赤くならない**状態だった。
  審査は同時に、元からあった検出器の不具合も2件見つけた:
  ①「入浴の様子」の「様」を敬称と誤認し、入浴・夜間・食事が人名候補になる（会議1本で数百か所が
  赤くなり、1か所ずつ確かめること自体が不可能）②主治医の「◯◯先生」を1件も拾えない（実測ゼロ）。
- **対策**:
  ①畳んだ欄は「赤い言葉＋前後40字」を全部並べる形へ（`candidateContexts`）。隠すのは地の文だけ
  ②全文を一度も開かずに送ろうとしたら、送る前に一度だけ知らせる
  ③敬称の直後が `子式相々態` なら数えない／敬称に「先生」を追加（`candidates.ts`）
  ④**画面そのものを検査するテスト**を新設。`react-dom/server` の `renderToStaticMarkup` で
  既定の描画を固定する（jsdom 不要・7本）。審査エージェントが使った手口をそのまま道具にした
  ⑤畳む目安を 1200 → 3000字（1200だと手書きメモ1500字まで畳み、録音を使わない職員には後退）
- **ファイル**: `components/drafts/PreSendPreview.tsx` / `.test.tsx` / `lib/privacy/previewNav.ts` /
  `lib/privacy/candidates.ts` / `app/globals.css` / `app/api/transcribe/route.ts`
- **教訓**:
  (a) **「安全のための変更」ほど独立審査に出す**。作った本人は「良くした」と思い込んでいるので、
      弱くなった方向を自分では見に行かない。今回も自己点検では気づけていなかった。
  (b) **画面は画面として検査する**。純粋ロジックのテストが全部緑でも、描画の分岐1本で警告が消える。
      `renderToStaticMarkup` なら重い道具なしで「最初に目に入るもの」を固定できる。
  (c) **隠すものを決めるときは「何を確かめさせたいか」から決める**。量で機械的に切ると、
      確かめてほしいものから先に消える。
  (d) 長い文章を扱えるようにする変更は、**既存の短い入力への影響**も必ず測る（しきい値1200の件）。

### 2026-09-17: 独立審査の一時ファイルを `git add -A` で3回巻き込んだ
- **発生回数**: 3回（同日・R2 で2回、R3 で1回。3回目でついに commit・push まで通した）
- **問題**: 敵対検証グラフの審査エージェントは、指摘を実証するために作業ツリーへ一時的な
  テストファイルを置く（`__doubt_probe.test.ts` `zz_probe.test.ts` `zz_proof/p1.test.ts` など）。
  多くは自分で消すが、並行して走る他のエージェントの邪魔になるのを避けて**わざと残すこともある**。
  こちらはその最中に `git add -A` で commit したため、審査の道具がリポジトリに入った。
  テストとして有効なファイルなので CI も緑のまま通ってしまう（気づく手がかりが無い）。
- **対策**:
  ①`.gitignore` に観測済みの3つの規則（`**/__*probe*` `**/zz_probe*` `zz_proof/`）
  ②**審査が走っている間は `git add -A` を使わない**。`git add <パス>` で明示する
  ③commit 前に `git status --short` を読む（今回は読んでいたのに `??` を見落とした）
- **ファイル**: `.gitignore` / `.claude/steering-log.md`
- **教訓**:
  (a) **`git add -A` は「自分が作ったものだけ」を入れる操作ではない**。他のエージェントや道具が
      置いたものも等しく入る。並行作業があるときは、範囲を自分で決めて add する。
  (b) 名前の規則で防ぐのは補助にしかならない（審査側は毎回違う名前を使う）。
      **操作の側を変える**のが本筋。
  (c) `git status` を「見た」だけでは足りない。`??` の行を1つずつ声に出して確かめる。

### 2026-09-17: 「印を立てる側」はあったが「受け取って実行する側」が居なかった
- **発生回数**: 1回（独立審査が実物を動かして検出。§2.7-D により1回目で根治）
- **問題**: ブラウザ録音（R3）で、連続失敗時に「録音を止める」決まりを作った。状態の側は正しく
  `stopped` を立て、画面にも「録音を止めました」と出た。**だが実際には止まっていなかった** ──
  MediaRecorder は `recording` のまま、マイクも掴んだまま、時計も進み続けた。
  `queue.stopped` を読んでいるのは表示だけで、それを受けて `recorder.stop()` を呼ぶ処理が無かった。
  同じ形の欠落が他にも5件あった（止まったあと残りの区切りが宙に浮く／画面が固まる／
  マイクが外れると毎秒122回の暴走／やり直しが0ミリ秒で3連打／予算の式が上限を超えていた）。
  **純粋ロジックのテスト30本も、描いた HTML のテスト6本も、全部緑だった。**
- **対策**:
  ①`queue.stopped` を受けて `finish()` を呼ぶ処理を追加（他5件もそれぞれ根治）
  ②`components/recording/RecordingPanel.live.test.tsx` を新設 ── jsdom＋偽の MediaRecorder／
  マイク／通信で**画面を実際に動かす**。「止めましたと出たら recorder が inactive でマイクも
  離れている」を固定した。**配線を1本外すと赤くなることを確認**（外して赤・戻して緑）
  ③並列実行で落ちる問題（jsdom の起動が遅い）は、待ちを4回に増やし timeout を30秒にして解消
- **ファイル**: `components/recording/RecordingPanel.tsx` / `.live.test.tsx` /
  `lib/recording/segments.ts` / `config.ts` / `lib/transcribe/validate.ts`
- **教訓**:
  (a) **決まりを作ったら「誰が実行するか」を指差し確認する**。状態に印を立てるのは決まりの半分で、
      受け取って副作用を起こす側が無ければ、画面に文字が出るだけの飾りになる。
      しかも表示が正しいぶん、見た目では絶対に気づけない。
  (b) **上限の値が同じ2つの定数は、必ず一方が誤作動する**（やり直し3回・連続失敗3本）。
      同じ数字を別の意味で使うときは、片方だけの経路を通すテストを必ず書く。
  (c) **動かさないと見えない機能には、動かす検査を書く**。純粋ロジックに切り出せた部分が
      緑でも、配線が抜けていれば製品は壊れている。切り出しは検査の代わりにならない。
  (d) 審査に「実物を動かして証拠を出す」ことを期待してよい。今回の6件はすべて
      jsdom の実行出力付きで、こちらの確認が数分で済んだ。

### 2026-09-18: 既に持っている「正解」を使わず、同じ罠を2回目に踏んだ
- **発生回数**: 2回目（1回目＝関係者名簿の表・2026-09-11。2回目＝文字起こしの表・2026-09-17）
- **問題**: 新しい表（`client_transcripts`）を作る機能を出したが、**表が未作成のときの扱いを
  作り忘れた**。保存に失敗すると `null` を返すだけで、入口は「利用者が見えない」と区別できず、
  職員には**「権限がありません」**と出ていた。真因は「管理者が SQL を実行していない」で、
  職員には直しようがない。しかもマニュアルは職員に「表が未作成と出たら管理者に伝えてください」と
  教えているのに、その合図が出ない。
  **同じ罠の正解はリポジトリに既にあった** ── `RelatedTableMissingError` / `MISSING_TABLE_CODES`
  （42P01・PGRST205）/ 管理者を名指しする文言まで揃っていたのに、参照しなかった。
- **対策**:
  ①`MISSING_TABLE_CODES` を `lib/db/clients.ts` から export し、**判定を写さずに読む**形にした
  （3つ目の表が増えても1か所を見ればよい）
  ②保存・一覧・本文・削除の4経路すべてで 503＋管理者への指示に
  ③戻り値を `{ok:false, reason}` に分け、「見えない利用者」と「保存の失敗」を撃ち分ける
  ④入口テストに「表が未作成のとき 503＋SQL 実行を促す文言」を4本追加
- **ファイル**: `lib/db/transcripts.ts` / `lib/db/clients.ts` / `app/api/transcripts/` /
  `tests/api/transcripts.route.test.ts`
- **教訓**:
  (a) **新しい表・新しい外部依存を足すときは、「同じものを先に作った場所」を必ず読む**。
      steering-log と CONTEXT-MAP に答えが書いてあったのに、新規実装のつもりで素通りした。
  (b) 2回目に踏んだということは、1回目の対策が「そのコードの中」で閉じていた。
      **横展開できる形（export された定数・共有の関数）にして初めて再発防止になる**。
  (c) 失敗の戻り値を `null` 一本にすると、**意味の違う失敗が混ざる**。混ざったまま画面に出ると、
      職員は直せないものを直そうとする。理由を型で分ける。

### 2026-09-18: 画面だけが嘘をついていた（自動削除は無いのに「5年で消えます」）
- **発生回数**: 2回目（1回目＝2026-09-13「スマホに常時表示」と7か所に書いたが実際は消えていた）
- **問題**: 文字起こしの保存で、仕様・管理者手順・説明書はどれも「期限切れの自動削除は未実装」と
  正直に書いたのに、**職員が実際に読む画面だけ**が「5年で消えます」と断言していた。
  読む人がいちばん多い場所が、いちばん不正確だった。
- **対策**: 画面の文言を「5年を過ぎたら消す決まりですが、いまは自動で消えません（管理者が
  まとめて消します）」に。仕様 §9.4 の「正直な限界」と同じ内容を、同じ言葉で画面にも出す。
- **教訓**:
  (a) **正直さは書類ではなく画面で測る**。ドキュメントに限界を書いて満足すると、
      その限界を知らない人が使う場所だけが残る。
  (b) 「未実装」と書いた機能の**言い切り表現**を、画面側で grep して確かめる習慣を持つ
      （「消えます」「されます」「できます」）。

### 2026-09-23: 全体の CSS が画面中の余白を 0 にしていた（「もっさり」「左上にギュッと」の一因）
- **発生回数**: 1回目（実害＝全画面の見た目。§2.7-D で1回目から根治）
- **問題**: `app/globals.css` の `* { margin: 0; padding: 0 }` が **@layer の外**にあった。
  Tailwind v4 の余白指定（p-4 / px-3 / space-y-4 …）は `@layer utilities` に入るので、
  層の外の規則に**詳細度と関係なく負ける**。ボタンの文字が枠に貼り付き、カードの中身が縁に密着し、
  縦の間隔も消えていた。ビルドもテストも緑のまま、見た目だけが壊れる種類の不具合。
  吉本さんの「もっさり・AI臭い」という感想の一部は、デザインの好みではなくこの不具合だった。
- **対策**: 要素そのものを狙う規則（`*` / body / h1-h3 / focus-visible）を `@layer base` に入れた。
  センサー `app/globals.test.ts` ── 層の外に「要素の余白を決める規則」があれば落ちる。
  変異で実証: 直す前の globals.css → 2件赤、直した後 → 3件緑。
- **教訓**: 見た目の苦情は、まず**実際の描画が CSS の意図どおりか**を確かめる（好みの話に入る前に）。

### 2026-09-23: CI が6日間（14回連続）赤いまま、誰も気づかなかった
- **発生回数**: 2回目（1回目＝2026-09-13 別リポジトリで CI レビューが残高切れのまま失敗し続けた件）
- **問題**: R2（2026-09-17 01:54）で入れたテストの1行（`await last?.json()`）が Biome の error。
  CI の最初の段（biome）で止まるため、**以降の tsc・build・テストは GitHub 上で一度も走っていなかった**。
  手元で確かめたつもりだったが、`npx biome check . | tail` の形で走らせ、**終了コードを tail のものに
  すり替えて**読んでいた。終了時の自動チェック（stop-build-check.sh）も biome を走らせているが、
  exit 1 は作業を止めない種類なので、私の手を止めなかった。
- **対策**: 該当行を直した（`if (!last) throw` で絞ってから読む）。
  手元の確認は **パイプを挟まずに終了コードを取る**（`cmd > log 2>&1; echo $?`）に統一する。
- **未了（吉本さんの承認待ち ── ハーネス自体の変更は §2.7-F の二段ゲート）**:
  「CI が赤いのに次の push をしている」ことを機械で止めるセンサー。候補は
  ①stop-build-check.sh を exit 2（作業を止める）にする ②push 前に `gh run list` で直前の CI を確かめる。
- **関連**: 審査エージェントの検証用ファイルが**4回目**で残っていた（`__doubt_live4` / `__lens3live` /
  `zzlens1b.probe`）。名前の規則が毎回ずれるので、.gitignore を「`__` か `zz` で始まるテストファイル」まで広げた。

### 2026-09-23: 画面の安全テスト3件が、壊れても緑のままだった（空振りの検査）
- **発生回数**: 同じ種類（検査は緑なのに守れていない）の2回目（1回目＝2026-09-17 送る前の画面で赤い言葉が消えても緑）
- **問題**: 作業台（A案）へ作り直す前の点検で、描いた HTML を**文字列のまま**照合していた検査が空振りしていた。
  ①「録音を始める」の disabled を `/<button[^>]*disabled[^>]*>/` で見ていたが、ボタンの class にある
  Tailwind の `disabled:opacity-50` の文字で満たされ、**同意なしで押せる状態でも緑**
  ②畳んだ欄の理由「（敬称の前）」を html 全体の `toContain` で見ていたが、`<mark title="敬称の前">` の
  ふきだしで満たされ、**画面の文字が消えても緑**（タッチ端末ではふきだしが出ない）
  ③帯の件数 `3</span>か所` は欄が1つの例しかなく、**合計と欄ごとの数を区別できなかった**
  さらに名簿の共有状態の表示（SharingStatus）には検査が1つも無かった。
- **対策**: テスト＋道具。`tests/helpers/markup.ts`（parse5 で HTML を木として読み、属性の中身を画面の文字に数えない）を足し、
  3件を木で読む形に直した（①disabled 属性そのもの ②その赤い言葉と同じ行の、属性ではない文字 ③2欄に2＋1か所の例で、帯に合計3）。
  帯が「出ること」の検査も足した（これまでは「出ないこと」だけ）。`components/SharingStatus.test.tsx` を新設（3状態×2種類）。
  部品をわざと壊して7通りとも赤になり、以前の照合は①②③の壊し方で緑のままだったことを確かめた。
  道具そのものの検査 `tests/helpers/markup.test.ts` と SharingStatus の検査を安全テストの一覧に載せた。
- **ファイル**: `components/recording/RecordingPanel.test.tsx` / `components/drafts/PreSendPreview.test.tsx` /
  `components/SharingStatus.test.tsx` / `tests/helpers/markup.ts` / `tests/helpers/markup.test.ts` / `tools/safety-tests.json` / `package.json`（parse5 を開発用に明記）
- **教訓**:
  (a) **画面の検査は、文字列ではなく木で読む**。属性（class・title）の中の文字と、画面に出る文字を混ぜると空振りする。
  (b) 新しい検査は、**部品を壊して赤になるのを見るまで**信用しない。緑になったことは証拠にならない。
  (c) **道具の説明は、壊して確かめた範囲だけを書く**。「画面に出る」は CSS まで見ないと言えない言葉なので、見ている印を列挙する（下の追記）。
- **追記（同日・検収の指摘 ── 同じ種類の3回目）**: 初版の `textOf` の説明は「画面に出る文字だけ」だったが、
  実際は属性を除くだけで、`hidden` 属性・class の `sr-only`・`aria-hidden` の中の文字も数えていた。
  理由の span を `hidden`／`sr-only` にしても「文字で読める」検査が緑、共有状態の注意書きを `sr-only` にしても緑だった
  （説明が事実でない ── 横断規約 §2.5-C。後の見た目の作り直しがこの説明を頼りにする）。
  **対策**: テスト＋道具。`textOf` を「隠す印（`hidden` 属性・`aria-hidden="true"`・class の `hidden`/`invisible`/`sr-only`・
  style の `display:none`/`visibility:hidden`）のある要素の文字を、その要素自身・祖先・入れ子のどこに印があっても数えない」に直し、
  道具の検査に隠す印7通り・祖先の印・似た書き方（`overflow-hidden`・`md:hidden` など）を足した。
  隠した文字を数えなくなったぶん「出していない」の検査が甘くなる（隠して出すと緑）ので、共有状態の「出さない」は HTML 全体で見る形にした。
  判定しないもの（CSS ファイル側の見え方・`md:hidden`・閉じた `<details>`）は `textOf` の説明に書いた。
- **追記2（同日・検収の指摘 ── 検査の名前が、見ている範囲より広かった）**: 「なぜ赤いか」が文字で読める の検査は
  畳んだ欄（3000字超）しか見ていなかった。開いた欄（3000字以下の欄と、全文を表示した欄）では理由が `<mark title>` の
  ふきだしにしか無く、**タッチ端末の職員には理由が1つも見えていなかった**（以前からの穴。検査の名前がそれを隠していた）。
  **対策**: 製品＋テスト＋ガイド。開いた欄も本文の下に言葉ごとの理由を文字で出す（`redWordReasons`・本文には差し込まない）。
  理由の文字は `--faint`（白地で 3.8:1）から `--muted`（7.9:1）へ。検査は「畳んだ欄」「開いた欄」の2つに分け、
  開いた欄は「言葉と理由の組」「本文は送る文章そのまま」「赤い印は出てくる回数どおり」を縛った。
  部品を壊す7通り（一覧を出さない・hidden・ふきだしだけ・本文に差し込む・言葉でまとめない・理由の取り違え・畳んだ欄の理由を消す）がすべて赤。
  CLAUDE.md に「検査の名前は検査した場合だけを言う。画面が場合分けしているなら場合ごとに検査を置く」を足した。
  教訓 (d): **名前が広い検査は、その外側の穴を「守られている」ように見せる**。検収では名前と中身の範囲を突き合わせる。
- **追記3（2026-09-24・検収の指摘 ── 追記1と同じ穴が、文字ではなく部品の数え方に残っていた）**: `textOf` は隠した文字を数えなくなったが、
  `elementsOf` は隠した要素も返す。共有状態の検査は切り替えの部品を `elementsOf` で数えていたため、包む div に `class="hidden"` や
  `aria-hidden="true"` を付けても「切り替えが出る」が緑だった（作業台で切り替えを上の帯へ移し、画面幅で隠し分ける F4 の前に塞ぐ必要がある）。
  録音の同意の文・「音声は保存しません」「一時停止」も html 全体の `toContain` のままで、文の span を `sr-only` にしても緑だった。
  **対策**: テスト＋道具。`isReachable`（自分と祖先の隠す印を見る。`textOf` と同じ判定）を足し、切り替えは届くものだけ数える。
  同意の文は「チェックの印と同じ label の中の届く文字」、説明の2語はパネルの届く文字で見る。
  横展開: 送る前の画面の赤い印も `<mark` の数と html 全体の言葉で見ていたので、届く印の言葉（`marksOf`）で見る形にした。
  教訓 (e): **文字の穴を塞いだら、同じ判定を「要素を数える」側にも当てる**。道具に穴を塞いだ関数を足したら、`elementsOf` を数えている箇所を洗う。

### 2026-09-23: 安全テストの見張りが、テストの書いた偽の集計行で緑になった
- **発生回数**: 同じ種類（検査は緑なのに守れていない）の3回目。見張りそのものに出たのは初めて
- **問題**: `tools/run-tests.mjs` は vitest の stdout と stderr を**つないだ文字**から、最後の集計行を読んでいた。
  テストの `console.error` は stderr に出るので、つなぐと vitest の本物の集計（stdout の末尾）より**後ろ**に来る。
  検収で `it.skip` を1件入れ、別のテストで `console.error("      Tests  628 passed (628)")` と書くと、
  本物は「627 passed | 1 skipped (628)」なのに「飛ばされたテストもありません」で終了コード 0 になった（手元でも再現）。
  説明（`parseSummaryLine` の「最後の集計行を使う」）は、stderr が混ざると成り立たなかった（§2.5-C: 説明が事実でない）。
- **対策**: テスト＋センサー。①集計行は **stdout だけ**から読む（`checkRunSummary`）。`Errors` 行は増えても「落ちる」側なので両方から探すまま
  ②引数なしの実行では、JSON レポート（vitest がテストの状態から直接書くファイル）を**2つ目の判定**にした
  （全ファイルが走り・全テストが合格でなければ失敗。読めなければ失敗 ── `checkReportDetails`）
  ③`tools/testManifest.test.ts` に stderr の偽の集計行・stdout の偽の集計行・stderr にしか集計が無い場合の検査を足し、
  以前の読み方（つなぐ）に戻すと赤になること、④を止めても⑤が偽の集計行の下の skipped を名指しで止めることを、実際に走らせて確かめた。
- **教訓**: **合否を「画面に出る文字」で決めるときは、その文字を誰が書けるかを考える**。テスト自身が書ける場所（stderr・stdout の途中）の文字は、
  判定の材料にしない。書けない材料（ツールが直接書くファイル）があれば、2つ目の判定に使う。

### 2026-09-24: DB の失敗を「無い・空・見つからない」と答えていた（同じ種類の3回目）
- **発生回数**: 3回目（1回目＝2026-09-18 文字起こしの表が無いのを「権限がありません」、2回目＝U0 988cc85 `getClients` の `[]`、
  3回目＝S1 b9d7d35 の検収で `getClientById` の `null`）。3回目を直したコミットの検収で、同じ書き方が**隣の7関数**に残っていると指摘され、lib/db 全体を grep すると評価の履歴の2関数にもあった（計9関数）
- **問題**: `lib/db` の関数が DB の失敗を 0件と同じ値で返し、入口がそれを「ありません」「見つかりません」と言い換えていた。
  `getRelatedPeople`・`getTranscriptsByClient`・`getDocumentsByClient`・`getEvaluations` は `[]`（画面は「家族は未登録」「保存した書類はありません」「評価 0件」）、
  `getTranscriptText`・`approveDocument`・`unapproveDocument` は `null`（404「見つかりませんでした。」「書類が見つかりません。」）、
  `deleteTranscript` は読み出しの失敗も**消す操作の失敗も** `false`（消えていないのに「見つかりませんでした」＝もう無い、と伝わる）。
  1件を読む `single()` は0件にもエラー（PGRST116）を返すので、`if (error || !data) return null` の形では2つを分けられない。
  あわせて、救済モードの保存を途中の失敗から押し直すと同じ方をもう1人登録していた（11426ed で修正 ── 二重登録の入口としても3回目:
  U0 の一覧の失敗・S1 の 404・押し直し）
- **対策**: ガイド＋センサー＋テスト。
  ①共通の部品 `lib/db/errors.ts`（`DbAccessError`〔`publicMessage`〕・`dbFailedMessage`・`isMalformedIdError`）を置き、
  `ClientLookupError` をその子にした。8関数を「0件は0件・失敗は投げる」に直し（1件は `maybeSingle`・削除は消えた件数まで見る）、
  7つの入口が 503 と `e.publicMessage` を返す。使われていなかった `getEvaluationById`（同じ書き方）は消した
  ②**センサー** `lib/db/dbFailures.test.ts`: Supabase を偽物にして問い合わせを1つずつ失敗させ、どの関数も「0件・見えない・空」と
  答えないことを確かめる（投げる／その関数が「失敗」と決めた値を返す／読み直して成功と同じ、のどれか）。
  `lib/db.ts`・`lib/db/*.ts` の async 関数が一覧に無ければ落ちるので、新しい関数でも再発を拾う。
  直した8関数を元の書き方に戻すと8件とも赤、一覧に無い関数を足すと赤になることを確かめた
  ③CLAUDE.md の Rules に「lib/db の読み書きは『0件』と『DB の失敗』を分ける」を足した
  ④入口の検査 `tests/api/entryErrors.route.test.ts` に `DB_FAILURE_ROUTES`（7つの入口・承認と取り消しを分けて8通り）を足した
- **ファイル**: `lib/db/errors.ts` / `lib/db.ts` / `lib/db/{clients,documents,transcripts}.ts` / `app/api/{history,clients/[id],clients/[id]/related,documents/[id],transcripts,transcripts/[id]}/route.ts` /
  `lib/db/dbFailures.test.ts` / `lib/db/{clients,transcripts}.test.ts` / `tests/api/entryErrors.route.test.ts` / `CLAUDE.md`
- **教訓**:
  (a) 2026-09-18 の教訓 (b)「横展開できる形にして初めて再発防止になる」を、定数（`MISSING_TABLE_CODES`）だけで止めていた。
      **判定を共有しても、使わない関数には効かない**。「全部の関数を列挙して、同じ故障を注入する」見張りにして初めて、隣の関数と新しい関数に届く。
  (b) 直した関数の**隣**を読む。検収の指摘は「その関数」を名指ししても、原因は書き方（`if (error || !data) return null`）にある。
      直す前に、同じ書き方を lib/db 全体で grep する。
  (c) 画面の側にも同じ壊れ方がある（一覧を読めなかったときに空に見せる）。サーバだけ直しても、画面が黙って空にすれば職員には同じに見える
- **追記（同日・画面の側）**: (c) のとおり、サーバが 503 を返すようにしても3つの画面が失敗を捨てていた。
  関係者名簿（`RelatedPeople` は `resp.ok` でないと何もせず空の名簿）・保存した文字起こし（`SavedTranscripts` は欄ごと消える＝「無い」）・
  ダッシュボード（`Array.isArray` でなければ捨て、「まだ評価履歴がありません」と件数 0）。
  **対策**: 3画面とも読めなかったことを `role="alert"` の文字で出し（件数は 0 でなく「—」）、名簿と文字起こしは「もう一度読む」を置いた。
  文字起こしの「消す」の失敗はサーバの文（「消せませんでした（消えていません）。」）をそのまま出す。
  検査= `tests/ui/clientListErrors.live.test.tsx`（3画面を jsdom で動かす。元の部品に戻すと失敗の5件が赤・0件の対照3件は緑のまま）
