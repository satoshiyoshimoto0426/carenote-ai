# 仕様: 電話連絡パイプライン（文字起こし → 黒塗り → 要約 → 支援経過・カレンダー・アセスメント）

> 作成: 2026-09-09 / 吉本さん決定「CareNote AI の機能として実装する」を受けた段階計画（§2.8-A ①定義）。
> 根拠調査: [../CALL-PIPELINE-FEASIBILITY.md](../CALL-PIPELINE-FEASIBILITY.md)。境界は SPEC §12 を継承（**確定は常に人**）。
> 位置づけ: リスキリング研修 中級編の目玉（A＋B1）。上級編（B2）は本書の第5段。

## 0. 一言で

電話の内容を、**建物の外に出す前に黒塗りし**、AIに下書きを作らせ、**職員が見て押してから**介護ソフト・カレンダーへ書く。

## 1. 5段階（1段ずつ動かして確かめる）

| 段 | 何ができるようになるか | 外に頼るもの | 状態 |
|---|---|---|---|
| **1. 黒塗りの強化** | 名前だけでなく、電話番号・住所・生年月日・番号類も送る前に消す。消し忘れがあれば送信を止める | なし（純粋ロジック） | **着手（本PR）** |
| 2. 送る前に見る画面 | 「AIに送る文章」を画面に出し、消し切れなかった候補を赤く示す。職員が押して初めて送る | なし | **実装済（実機確認待ち）** |
| 3. 電話の文字起こし入口 | 録音ファイルを外部サービスで文字にし、支援メモへ足して通常の「送る前に見る」へ乗せる | **D1 決定＝外部サービス（OpenAI 文字起こしAPI）**・鍵の設定 | **実装済（鍵待ち）** |
| 4. Googleカレンダー | 要約から予定を抽出→「Googleカレンダーに追加」リンク（内容入りの作成画面を開く）＋ .ics。保存は職員が押す。タイトルは記号＋用件のみ | **D2 決定＝個人 Google**（API・審査を使わない） | **実装済（実機確認待ち）** |
| 5. アセスメント追記（上級） | CareNote が「どの欄に何を足すか」の案を作る → 拡張が「前後を見る→退避して末尾に追記→元に戻せる」。登録は人 | カイポケ実機・規程 | **実装済（実機確認待ち）** |

## 2. 第1段の仕様（本PR）

### 2.1 黒塗りの3段構え

```
入力テキスト
  → ① 名簿置換 maskNames（登録利用者。既存）
  → ② 型置換  maskPatterns（電話・郵便番号・メール・住所・生年月日・番号類）
  → ③ 自己点検 assertNoLeak（登録実名が1件でも残っていれば例外＝送信中止）
  → AIへ
```

- ①②を束ねる入口は `lib/privacy/maskPii.ts` の `maskPii(text, aliases)`。戻り値は `{ text, findings }`（findings は**種類と件数のみ**。原文は持たない＝ログに個人情報を残さない）。
- ③は `lib/privacy/leakCheck.ts`。`PiiLeakError` を投げ、API は **422** で「置換に失敗したため送信を中止」を返す（fail-closed）。
- 全角英数字は NFKC 正規化で半角に揃えてから照合する（「０９０−１２３４」も拾う）。

### 2.2 型置換の対象と、わざと消さないもの

| 消す | 例 | 置換後 |
|---|---|---|
| 電話番号 | 090-1234-5678 / 06(1234)5678 / 0120-123-456 / 090.1234.5678 / 090ー1234ー5678 / +81 90-1234-5678 / TEL:0901234-5678 | 〔電話番号〕 |
| 郵便番号 | 〒530-0001 | 〔郵便番号〕 |
| メール | a@b.co.jp | 〔メール〕 |
| 住所（番地まで） | 大阪府大阪市北区梅田1-2-3 | 〔住所〕 |
| 生年月日 | 「生年月日 1950年3月4日」「生年月日 1950/3/4」「昭和25年3月4日」 | 〔生年月日〕 |
| 番号類（8〜12桁の連番・4桁-4桁-4桁） | 被保険者番号 0123456789 / 1234 5678 9012 | 〔番号〕 |

**消さない**：予定の日付（「9月12日に面談」）。消すと第4段のカレンダー登録と支援経過の日付が壊れる。生年月日は「生年月日／生まれ／誕生」の文脈語がある場合と、昭和・大正・明治の年月日のみ対象。

**置換と検査を別系統にする（独立審査 2026-09-11）**：上表のルールで拾えない番号（0落ちの「90-1234-5678」など）も、漏れ検査は「区切りを無視して10桁以上の数字が続く」だけで止める（`hasLongDigitRun`）。ルールの取りこぼしが検査の取りこぼしにならない。実名は空白・ゼロ幅文字・旧字体（髙／高 等）の揺れを許して探す（`nameRegex`）。

**正直な限界（型置換）**：都道府県名の無い住所（「大阪市北区梅田1-2-3」）は拾わない（「大阪市北区の事業所に3回」まで消してしまうため）。名前候補の赤下線と同じく、第2段の「人が見る」で止める。

### 2.3 受け入れ条件

- [x] 上表の各型がテストで置換される。予定の日付は残る（`patterns.test.ts`）
- [x] 登録実名が残った文章を渡すと `assertNoLeak` が例外を投げる（`leakCheck.test.ts`・`maskPii.test.ts`）
- [x] `/api/generate` `/api/rescue` が `maskPii` を通り、例外時に 422 を返す
- [x] サーバログに黒塗り前の本文が出ない（件数のみ・`findings` に原文を持たないことをテストで確認）
- [x] 既存120＋新規17＝137テスト緑、変更ファイルの Biome 合格、`npx tsc --noEmit` 合格（2026-09-09）
- [ ] 実機で「電話番号入りのメモ→生成」を通し、送信本文に番号が無いことを確認（吉本さん・第2段の画面で）
- [x] ルート層のテスト（`tests/api/*.route.test.ts`）：名簿が読めなければ 503・実名が残れば 422・AI に渡る本文に実名と番号が無い（2026-09-12・独立審査の指摘で追加）

> 既知の別件: `npm run check` はリポジトリ既存の config ファイル（CRLF）と a11y 警告で赤。本PRの変更ファイルは合格。別PRで整理する。

### 2.5 二枚方式（吉本さん決定 2026-09-09）── 「消す」と「捨てる」は別

吉本さんの懸念「全部消すと本当に必要な情報を聞き逃す」への答え。黒塗りは**AIに見せる紙**にだけかけ、元の値は手元が覚える。

```
AIに送る紙   「A様の長女から電話。折り返し〔電話番号1〕。9/12 14時に面談希望」  ← 送信後は残さない
手元の札入れ  〔電話番号1〕＝090-xxxx-xxxx（リクエスト内のメモリのみ。ログ・DB・AIへ出さない）
AIの返事     「A様の長女より連絡。折り返し先〔電話番号1〕」
手元で戻す   「A様の長女より連絡。折り返し先 090-xxxx-xxxx」  ← 職員に返る（フル版）
```

- 札は種類ごとに連番（〔電話番号1〕〔住所1〕）。同じ値には同じ札。1リクエスト内の全フィールドで札入れを共有し、番号を衝突させない（`lib/privacy/vault.ts`）。
- AIの返事（帳票JSON）は `restoreDeep` で文字列をすべて戻してから返す。**ただし予定（`appointments`）は戻さない**（カレンダーには記号＋用件のみ・2026-09-12）。
- **戻した帳票を再び AI へ送る API は、必ずもう一度黒塗りを通す**（`maskBody.maskDeep`）。`/api/kaipoke/assessment` は `/api/generate` が戻した下書きを受け取るため、入れ子すべてを黒塗りし直してから AI へ渡す（独立審査 2026-09-11 critical #7 で発見・同日修正）。
- **保存帳票（documents）の扱い**：名前は記号のまま。電話番号・住所は「手元に戻した版」がそのまま保存される（吉本さん決定「確認用の情報も蓄積したい」に沿う）。保存先は鍵つきの Supabase で、実名だけが暗号化分離。番号類まで札のまま保存するかは §3 の D5 で判断する。
- **名前（A様）は戻さない**：保存帳票は「記号で保持・実名を含めない」契約（`lib/db/documents.ts`）を維持。表示時の実名復元は第2段の画面で行う。
- フル版の置き場は **CareNote AI の利用者ページ**（鍵つき保管・閲覧記録）。私物の携帯メモアプリ（iCloud／Google同期）は推奨しない（安全管理措置・閲覧記録・退職時の持ち出しの3点で説明できない）。使う場合は業務用端末＋同期オフ＋規程明記が条件（上級編の範囲）。
- AIが札の形を崩した場合（括弧を落とす等）は戻らない。第2段の画面で「戻せなかった札」を表示して人が補う。

### 2.4 正直な限界（第1段では解けない）

- 名簿にない人名（家族・他事業所の担当者）は消えない → 第1b段で「関係者名簿」を追加、上級で固有表現抽出（GiNZA）
- 「お母さん」「隣の田中さん」は消えない → 第2段の「人が見る」でしか止められない
- PDF原本の実名（救済モード）は本書の対象外（別宿題）

## 2b. 第2段の仕様（実装 2026-09-09）

- `POST /api/preview`：`/api/generate` と**同じ** `maskRequestBody` を通した本文を返すだけ（AIへ送らない）。名前っぽいのに消せなかった言葉を `findNameCandidates` で候補として添える。
- `lib/privacy/candidates.ts`：敬称（さん・様・氏…）の直前と施設語尾（病院・デイサービス…）という「形」だけで拾う。続柄・役割（お母さん・ケアマネさん）は除外。**確率的で取りこぼしあり**＝置換せず「示す」だけ。上級版の固有表現抽出までのつなぎ。
- `components/drafts/PreSendPreview.tsx`：黒塗り後の本文をそのまま表示、候補を赤い下線で示す。「戻って直す」／「この内容でAIに送る」。
- `/create`：生成ボタン→ `/api/preview` → 確認画面 → 「この内容で送る」→ `/api/generate`。画面で見せた文章とAIに送る文章は同じ関数で作る。

受け入れ条件：
- [x] 候補検出のテスト（家族名・担当者名を拾う／記号と札は拾わない／続柄は拾わない／施設名を拾う）
- [x] 本番ビルド合格・tsc 合格・149テスト緑
- [x] **実機**：ログイン後 `/create` で「送る前に確認」画面が出た（2026-09-09 吉本さん実機・`POST /api/preview 200`・番号1件置換）。送信は Anthropic の残高不足で 500 → 402 に改善（残高追加後に再確認）
- [x] 名簿が読めない時は送らない（`AliasLoadError` → 503）。実機で Supabase の一時エラー "JWT issued at future" が出て名簿なしで進んだ穴を同日に根治
- [x] 名前（A様→実名）の表示時復元：`GET /api/clients/aliases`（記号→実名・ログイン職員のみ・no-store）＋ `/create` の「実名で表示／記号で表示」切替。**画面とコピーにだけ**使い、AIにも保存にも渡さない（`restoreNamesDeep` は表示専用）。実機確認は吉本さん

## 2c. 第3段の仕様（実装 2026-09-09・D1＝外部サービス）

採用：**OpenAI 文字起こしAPI**。公式ページで確認した根拠（2026-09-09）：
- Claude API は音声入力に非対応（機能一覧はテキスト・画像・PDF）→ Anthropic では文字起こしできない
- OpenAI：既定で学習に使わない／**音声の文字起こし（/v1/audio/transcriptions）は監視ログの保持「なし」・アプリ状態の保持「なし」**（公式データ管理ページ 2026-09-12 確認。他の API の既定「最大30日」とは別）／25MB・mp3/mp4/mpeg/mpga/m4a/wav/webm／話者分け用モデルあり／保存地域は日本を含む選択肢（条件あり）／言語指定は `gpt-transcribe` では `languages`（複数形・`language` と両方送らない）
- Gemini：**無料枠は入力が製品改善に使われ人が読む**（有料枠は使われない）→ 事故の入口になるため不採用

作り：
- `lib/transcribe/provider.ts`（差し込み口。他社へ切替可能）／`validate.ts`（形式・25MB・エラーの言い換え）
- `POST /api/transcribe`：音声を受け取り外部へ渡し、文字だけ返す。**音声は保存しない**。ログは大きさと所要時間のみ
- `/create` 支援経過の欄に「録音ファイルから文字にする」。結果は支援メモの末尾に「【録音の文字起こし】」として足す → そのまま第2段の「送る前に見る」へ（黒塗り・赤下線・人の確認）
- 鍵は `OPENAI_API_KEY`（未設定なら「設定がありません」で止まる）

説明書への影響（**要更新**）：`docs/DATA-HANDLING-EXPLANATION.md` の再委託先に **OpenAI, L.L.C.（米国・音声の文字起こし）** を追加し、「録音した音声が外部へ出る」ことを利用者・家族への説明と同意書に反映する。

受け入れ条件：
- [x] 形式・大きさの判定と、外部エラーの言い換えのテスト
- [x] tsc・biome・vitest 緑
- [x] **API疎通**：鍵設定後、合成した日本語の電話音声（約10秒・mp3）を本番と同じ呼び出しで送信 → HTTP 200・2.8秒・全文一致で文字起こし（2026-09-09）
- [ ] **実機**：`/create` で「録音ファイルから文字にする」→ 文字がメモに入る → 「送る前に確認」で番号が札になる（吉本さん）
- [ ] DATA-HANDLING-EXPLANATION.md の更新（専門家レビュー前提）

### 2d. 録音機器の決定（2026-09-09・吉本さん）

**Genspark SecondBrain Note（個人プラン）・使い方B**＝レコーダー側で文字にし、全文を支援メモへ貼る。CareNote→OpenAI 経路は予備として残置。
Claude の懸念（プライバシーポリシーに「機微情報は収集しない」・音声がさらに不特定のAI事業者へ・個人プランに学習不使用の明文なし・DPA なし）は
提示済みで、判断は `~/.claude/decisions-log.md` に記録。説明書は DATA-HANDLING-EXPLANATION.md v0.3 §5-2 に懸念ごと明記。

## 2e. 第4段の仕様（実装 2026-09-09・D2＝個人の Google アカウント）

**API を使わない**：外部向けアプリで Calendar の権限（sensitive スコープ）を使うと Google の審査が必要で、審査前の「テスト」状態は
リフレッシュトークンが **7日で失効**する（公式ドキュメントで確認）。個人アカウント前提ではこれが日常運用に耐えないため、
①Google カレンダーの作成画面を内容入りで開くリンク ②標準形式 .ics の2本にした。どちらも**職員が保存ボタンを押して**登録される。

- `types/supportLog.ts` に `appointments`（title/date/startTime/endTime/location/note/confidence）。AI への指示：記号＋用件・実名や番号を書かない・年が無ければ「要確認」・過去の出来事は入れない。「今日の日付」を本文に渡して相対表現を直す
- `lib/calendar/links.ts`：日本時間→UTC、終日対応、既定60分、`maskPatterns` を二重の安全網として通す。`googleCalendarUrl` / `buildIcs`（RFC 5545）
- `components/drafts/AppointmentsPanel.tsx`：予定の一覧＋「Googleカレンダーに追加」「.ics」。**記号版の下書きから作る**（「実名で表示」中でも実名を渡さない）
- 既知：Google のリンク形式（`calendar/render?action=TEMPLATE`）は公式ドキュメントに無い広く使われる形式。壊れた場合は .ics で代替

受け入れ条件：
- [x] 整形（JST→UTC・終日・要確認注記・番号の札化）とリンク／.ics のテスト
- [x] tsc・biome・vitest 緑
- [ ] **実機**：電話メモから予定が拾われ、「Googleカレンダーに追加」で作成画面が開き、件名が「A様 …」で保存できる（吉本さん）

## 2f. 第5段の仕様（実装 2026-09-10）── 過去の記録を消さないための3段

**CareNote 側（案を作る・書かない）**
- `types/supportLog.ts` に `assessmentUpdates`（field: mainComplaints／lifeHistory／overview＝拡張の `FIELD_MAPS.assessment` と一致、text、reason、confidence）
- AI への指示：メモに**状態像の変化**（心身・ADL・認知・服薬・家族状況・住環境・意向）が読める時だけ、該当欄の**末尾に足す1段落**を作る。冒頭に「日付 情報源:」。実名・番号・住所は書かない。変化が無ければ空
- `components/drafts/AssessmentUpdatesPanel.tsx`：案を表示し「この追記文をコピー」（どの介護ソフトでも使える貼り付け型）

**拡張側（カイポケの欄に足す）** `extension/src/adapters/kaipoke.js`
- `buildAppendedValue(existing, addition)`：末尾の空白を詰めて改行で足す。**同じ文が既にあれば null**（二重追記を防ぐ）。テスト付
- `previewAppend`：欄の今の文章と追記後を返す（**書かない**）
- `applyAppend`：今の文章を退避してから書く（保存はしない）
- `undoAppend`：退避した文章に戻す（登録前に限る）
- `content.js` に `CARENOTE_APPEND_PREVIEW / APPLY / UNDO`、`panel.js` に「前後を見る」「この欄に追記する」「元に戻す」の3ボタンと前後の見比べ表示

受け入れ条件：
- [x] 純粋関数のテスト（改行で足す・空・二重追記の防止）／tsc・biome・vitest 166 緑・build 合格・拡張スクリプトの構文と export を確認
- [ ] **実機**：カイポケのアセスメント P1 を開き、「前後を見る」で今の文章が正しく読めること →「この欄に追記する」で末尾に足されること →「元に戻す」で戻ること（吉本さん・**登録ボタンは押さずに**確認）
- [ ] **規程**：追記の運用（誰が承認するか・追記の書式・元に戻す期限）を事業所規程に置く（上級編の教材そのもの）

## 2g. D4 関係者名簿（実装 2026-09-10）

黒塗りは名簿にある名前しか消せない（§2.4）。家族・他事業所の担当者・主治医を**利用者ごと**に登録し、「A様の長女」のような記号へ置き換える。
- DB: `supabase/client_related.sql`（`client_related_identities`：client_id・relation・name_encrypted〔AES-256-GCM〕・created_by。同じ利用者に同じ続柄は一意）。**Supabase の SQL Editor で実行が必要**
- 名簿読込 `loadAliases` が関係者も含める。**関係者の表が読めない時も送信を止める**（一時エラー→503「少し待って」／表が未作成→503「管理者が supabase/client_related.sql を実行」）。旧「警告のみで続行」は家族名が消えないまま AI へ出る穴だったため 2026-09-12 に廃止（独立審査 critical #3）。記号は `relatedAliasCode(clientCode, relation)`
- API `GET/POST/DELETE /api/clients/[id]/related`（所有者チェック・実名は画面表示用のみ。DELETE は利用者IDでも絞り、0件なら 404）
- UI `components/clients/RelatedPeople.tsx` を利用者詳細ページに配置（続柄＋氏名 → 登録／削除。「→ A様の長女」と置換後の記号を表示）
- テスト：記号生成・登録した家族名がメモから消えて復元できる

受け入れ条件：
- [x] tsc・biome・vitest 168 緑・build 合格（`/api/clients/[id]/related` 登録）
- [ ] **実機**：SQL 実行 → 利用者詳細で「長女／佐藤 一郎」を登録 → `/create` の「送る前に確認」で「佐藤一郎さん」が「A様の長女さん」になる（吉本さん）

## 2h. 第6段 OCR統合（実装 2026-09-11）── 情報提供書・主治医意見書・看護サマリーを読んで統合し、転記する

吉本さん指示「OCRで画像を分析・統合して適切に転記」。救済モードの Stage0（PDF読解）を深化させた。**別のOCRサービスは増やさない**（画像の文字読み取りは同じ Claude 呼び出しで行う＝預け先を増やさない）。

- 入口：`/rescue` の参考資料に **画像（JPEG・PNG・WebP）** を追加。スマホで撮った紙でも可。資料ごとに**種別**（診療情報提供書／主治医意見書／看護サマリー／認定調査票／基本情報／その他）を選ぶ
- 読み取り `lib/generation/rescueIntake.ts`：種別ごとの「読みどころ」をプロンプトに埋め込み、出力を構造化
  - `facts[]`：分類（診断・既往歴／処方・服薬／医学的管理・留意点／心身機能・ADL／認知・精神・行動／栄養・食事・嚥下／排泄・皮膚／家族・介護力・住環境／サービス利用状況／本人・家族の意向／その他）＋本文＋**出典**＋日付
  - `conflicts[]`：資料どうしの食い違い（どの資料が何と言うか＋確認方法）
  - `documents[]`：資料ごとの読み取り報告（AIが判定した種別・判読の状態）。職員の指定と違えば cautions にも
- 転記：`composeIntakeNotes` が「食い違い（冒頭）→分類別の事実（出典つき）」のメモにして帳票一式生成（アセスメント・第1/2表…）の入力にする。**欄に写しやすい粒度**にしたのが要点
- 黒塗り：読み取り結果の全文章（facts・conflicts・documents 含む）と**資料のファイル名**に maskPii。資料の**原本は Anthropic へ渡る**（DATA-HANDLING §3 の明示した例外・処理後に Blob 削除。400/422 で止まった時も削除。削除に失敗したら画面に warnings を出す）
- 一時保管（Vercel Blob）：現在は**公開ストア**（URL を知っていれば誰でも読める仕様）に、乱数付き・拡張子だけの名前で置き、処理後に削除する。**非公開ストア（Private）への切替は吉本さんの判断待ち（§3 D6）**
- 画面：資料ごとの読み取り報告（緑＝良好／赤＝判読不能）・食い違い（赤枠・確認方法つき）・事実（分類別・折りたたみ）

受け入れ条件：
- [x] 種別ヒント・分類別メモのテスト／tsc・biome・vitest 171 緑・build
- [ ] **実機**：主治医意見書と看護サマリーを写真で添付し種別を選ぶ → 読み取り報告が「良好」→ 事実が分類別に出典つきで出る → 食い違いがあれば赤枠に出る → 生成されたアセスメントに処方・ADL が反映されている（吉本さん・**テスト用の書類で**）
- [ ] 手書きの読み取り精度の実測（かすれ・傾き）。悪ければ「撮り方の案内」を画面に足す

## 2i. カイポケ転記シート（実装 2026-09-11）── アセスメントを10ページの欄に合わせて出す

根拠：[../KAIPOKE-TRANSCRIPTION-SPEC.md](../KAIPOKE-TRANSCRIPTION-SPEC.md) §1・§6「CareNote は段階1・2に集中し、カイポケのページ順・欄名・文字数を守った形で出す」。

- `lib/kaipoke/assessmentLayout.ts`：10ページ・31欄の自由記述欄（フォーム名・最大行×字・安全上限＝行×字−行数・書き分けヒント）。純粋データ
- `lib/generation/kaipokeAssessment.ts`：アセスメント下書き → 欄ごとの文章（構造化出力・key は欄一覧の enum）。事実のみ・推測は `isInferred`・個人情報禁止・禁止文字回避・全欄1件ずつ（空でも返す）
- `POST /api/kaipoke/assessment`：記号版の下書きを受け取り、補足メモは黒塗り
- `components/drafts/KaipokeSheetView.tsx`：ページごとに折りたたみ、欄名・文字数/上限（超過は赤）・推測を含む印・欄ごとコピー・全ページコピー・**拡張用JSONをコピー**
- 拡張：documentType `kaipokeAssessment`。JSONを貼ると「N枚目を流し込む」ボタンが出て、**開いているページ分だけ**書く（同じ欄名が別ページにあるため）。すでに文章が入っている欄には書かない（消さない）。推測を含む欄は黄色

受け入れ条件：
- [x] 欄一覧の一意性・安全上限・文字数チェック・テキスト整形・プロンプト・key変換のテスト／tsc・biome・vitest 183 緑
- [ ] **実機**：アセスメント下書き→「カイポケの欄に合わせる」→ 欄ごとにコピーして貼る／拡張用JSONを貼って「4枚目を流し込む」（吉本さん・テスト利用者）
- [ ] 文章の質（欄への振り分けが現場感覚と合うか）は吉本さんの目で較正 → ヒント文を直す

## 2j. 第2表の半自動化（実装 2026-09-11）── 6階層を「1手順ずつ・登録は人」で

根拠：[../KAIPOKE-TRANSCRIPTION-SPEC.md](../KAIPOKE-TRANSCRIPTION-SPEC.md) §4（ニーズ→長期→短期→サービス内容→種別→事業所、1サービス行あたり3画面、登録ボタンは実クリック）。

- 拡張は**画面遷移も登録も押さない**。パネルが「次にどの『+項目を追加』を押すか」を案内し、追加画面が開いたら「この画面に流し込む」でその画面の欄だけを埋める。職員が「登録する」→一覧に戻ったら「次へ」
- `buildPlan2Steps`（純粋）：CarePlanDraft の needs[] から手順列。`plan2ScreenFromNames`（純粋）：欄名と URL から今の画面を判定し、手順と違えば**書かずに知らせる**
- `parseFrequency`（純粋）：「週2回」→ 01（週・2回）／「毎日」→ 02／その他→ 03。`classifyServiceType`（純粋）：介護保険サービス語なら 01（種別セレクトを表示文字で選ぶ・保険給付○を ON）、本人・家族・医療機関は 02（任意サービス名）
- 事業所：取引先セレクトを表示文字（部分一致）で探し、無ければ「その他」＋事業所名（`writeField` が keyup も送る＝隠し欄同期）。期間は自由記述（〜は～に正規化）
- 長期・短期の**期間セレクト（年月日）は手で**（案内を出す）。進み具合は `chrome.storage.local` に保存（途中で閉じても続きから）
- 未対応：第3表（ポップアップ）。手入力

受け入れ条件：
- [x] 手順列・頻度・種別判定・画面判定のテスト／biome・vitest 188 緑・node --check
- [ ] **実機**：テスト利用者の第2表で、ニーズ1件＋サービス1件（=6手順）を通し、各追加画面で欄が埋まり、登録後に一覧の階層が正しいこと（吉本さん）
- [ ] 取引先セレクトの部分一致が実データの表記（【訪問看護】（番号）　名称）に合うか

## 2k. 独立審査（graph-doubt）の反映（2026-09-12）

main 取り込み前に別の AI に疑って見させた（§2.7-E・敵対検証グラフ・30エージェント・確定8件すべて critical）。反映内容：

| # | 指摘 | 直し |
|---|---|---|
| 1 | `vault.ts` に見えない NUL 文字が入り git がバイナリ扱い＝差分がレビューに届かない | 可視文字の区切りに変更。**センサー** `tools/check-invisible.mjs`（NUL・ゼロ幅）を CI と Stop フックに追加 |
| 2 | ブランチ未 push・PR 無し（二段ゲートの出口未通過） | 本節の修正後に PR 作成・claude-triage の結果を読んでからマージ |
| 3 | 関係者名簿の読み取り失敗が警告だけで続行（家族名が消えない） | 利用者名簿と同じく 503 で止める。表が未作成なら管理者向け文言。テストで固定 |
| 4 | fail-closed を外しても全テスト緑 | `lib/db/clients.test.ts`（偽 Supabase）＋ルート層テスト4本を追加 |
| 5 | SSRF 許可リスト・資料の再黒塗り・vault 復元を消しても緑 | `lib/rescue/sourceDocs.ts` に切り出してテスト。ルート層テストで固定 |
| 6 | 資料の原本を公開ストアへ元ファイル名のまま | 乱数付き・拡張子だけの名前に変更。非公開ストア化は D6（吉本さん判断） |
| 7 | `/api/generate` が戻した実番号入りの下書きを `/api/kaipoke/assessment` がそのまま AI へ | `maskDeep` で入れ子すべてを黒塗りし直してから送る。テストで固定 |
| 8 | 「ー」区切り・ドット・+81・TEL: 直後の番号を素通し（検査も同じ正規表現で沈黙） | 置換ルール追加＋別系統の「10桁以上の数字列」検査。名前は空白・旧字体の揺れも吸収 |

未反証で残った 37 件のうち、コードで直せるもの（拡張の再ログイン検知に第2表を含める・DOM 経路の jsdom テスト・.ics の DTSTAMP と折り返し・`languages` パラメータ・関係者 DELETE の 0件判定・予定を戻さない・ファイル名の黒塗り・削除失敗の警告・拡張経路の型置換・パネル文言）は同 PR で反映。判断が要るもの・差分外のものは GitHub Issue にした（D5〜D6 と Issue 一覧は PR 本文）。

## 3. 吉本さんの判断が要るもの

| # | 判断 | 影響する段 |
|---|---|---|
| D1 | 文字起こしを**事業所のPC内**で行うか、クラウドAPIに送るか | 3 |
| D2 | Google Workspace を事業所ごとに用意するか（組織内限定なら審査不要） | 4 |
| D3 | SMS社への規約照会を送るか（文面は作成済み） | 5・拡張全体 |
| D4 | 関係者名簿（家族・関係先）を利用者ごとに登録するUIを第1b段で作るか | 1b |
| D5 | 保存帳票に電話番号・住所を「戻した版」で残すか、札のまま残して表示時だけ戻すか（現状＝戻した版で保存） | 2・保存 |
| D6 | 資料の一時保管（Vercel Blob）を非公開ストアに作り直すか（作成後は公開⇄非公開を変えられない・`@vercel/blob` 2.3 以上で対応）。それまで OCR 統合を実運用に出すか | 6 |

## 4. 変更履歴

- 2026-09-12 独立審査（graph-doubt）の critical 8件＋未反証の実装可能分を反映（§2k）。D5・D6 追加。
- 2026-09-09 初版。第1段着手。
