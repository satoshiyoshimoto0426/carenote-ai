# A案「作業台」── main へ出す前に、ログインした画面で確かめること

**このファイルの役目**: 作り直し（ブランチ `redesign/a`）は途中で本番に出さず、最後にまとめて出す（吉本さん決定 2026-09-23）。
作業するエージェントはログインできない（パスワードを入れない決まり）ので、**ログインが要る画面の見た目は確かめられない**。
その「まだ誰も見ていない」確認を、忘れないようにここへ積む。**全部に「済」が付くまで redesign/a を main へ出さない。**

- 各段（スライス）は、ログインが要る確認を残したら、ここに1行足す（足さずに「確認済み」と書かない）。
- 確かめたら「状態」を `済（日付・確かめた人）` に書き換える。読めない・崩れている所があれば、その段へ差し戻す。
- エージェントでは直せず、**吉本さんが決めないと進まないこと**（デザインの決まりの例外にするか・お金がかかるか など）も、
  文章の中に書くだけにせず、ここに1行足す（状態は「未決定」。選んで、選んだ内容が画面に入ったら「済」）。
- ログインしなくても開ける画面（`/sign-in` `/sign-up`）は、エージェントが実画面で確かめて、その結果を「確かめたこと」に書く。
- 利用者の行はクリックしなくてよい（右側に関係者の実名が出るため。見た目の確認には要らない）。

| # | 段 | 確かめること | 吉本さんの手順（1〜3手） | 状態 |
|---|---|---|---|---|
| 1 | A1 色と書体 | `/clients` と、まだ作り直していない画面（`/create` `/evaluate` `/dashboard` `/rescue` `/guide`）で、**地に溶けて読めない文字が無い**こと。PC の幅（1440px 前後）とスマホの幅（375px 前後）の両方 | ① ログインして `/clients` を開く ② 左のメニューで各画面を開く ③ ブラウザの幅をスマホくらいまで狭めて同じように見る | 未確認 |
| 2 | A1 Clerk の層 | **事業所の切り替え**（A3 の外枠から**上の帯の右端**。事業所の印と下向きの矢印）を押すと出る一覧が読めること（影が消え、1px の線の枠になった）。左の帯の下端の自分の丸いアイコン（32px）を押すとメニューが開くこと（A3 で置き場所が変わったので書き換えた） | ① ログインして `/clients` を開く ② 右上の事業所の切り替えを押し、一覧が読めるか見る ③ 左下の丸いアイコンを押してメニューが開くか見る | 未確認 |
| 3 | A1 Clerk の押す場所 | ログインの**2段目**（メールを入れて「Continue」を押した後の画面）で、メールの横の**変更（鉛筆）ボタン**・「コードを再送」・「別の方法」などのボタンやリンクが、スマホで指で押しにくいほど小さくないこと（目安: 人差し指の腹くらい）。1段目の部品は 44px にして計測済み（下の「確かめたこと」）だが、2段目はフォームを送らないと出ないので、エージェントは計測できない。小さければ A1 へ差し戻す（`lib/clerkAppearance.test.ts` の `UNMEASURED_TOUCH_ELEMENTS` から `TOUCH_ELEMENTS` へ移して直す） | ① スマホでログアウトした状態から `/sign-in` を開く ② 自分のメールを入れて「Continue」を押す ③ 出てきた画面のボタンやリンクを指で押してみて、押しにくい所が無いか見る（ログインは最後までしてよい） | 未確認 |
| 4 | A1 Clerk のロゴ（**決めること**） | **吉本さんに選んでほしいこと**。ログイン・新規登録の枠の下にある「Secured by Clerk」（Clerk のロゴのリンク）は、押せる大きさが 48×14px で、スマホの押す場所の決まり（44px 以上）に届かない。Clerk が見た目の指定の口（elements の名前）を出していないので、今の設定では大きくできない（下の「確かめたこと」）。次の3つから1つ選ぶ ── **（ア）Clerk の表示として受け入れる**: 費用も作業も無し。この部品だけを 44px の決まりの例外として、ここに記録して終わる。**（イ）Clerk の内部の名前を狙った CSS で隠す**: 費用は無し。ただし Clerk が見た目の指定の口として約束していない内部の名前に頼るので、Clerk の更新で名前が変わると黙って効かなくなる（テストでは見張れず、画面で見直すしかない）。無料の契約でこの表示を CSS で消してよいかは、Clerk の規約をまだ確かめていない（未確認）。**（ウ）Clerk の契約を変える**: Clerk の公式の手順で消す ── ダッシュボードの Settings → Branding の「Remove "Secured by Clerk" branding」をオンにする（本番で使うには有料の契約が要る、と Clerk の公式ドキュメントにある）。有料の Pro は月 $25（年払いなら月 $20）── 2026-09-23 に clerk.com/pricing で確認。今の契約の種類は、このリポジトリに記録が無い（未確認）。契約の変更とスイッチはお金の手続きなので吉本さんが行う | ① 上の（ア）（イ）（ウ）から1つ選ぶ ② 選んだ記号をエージェントに伝える（この行の「状態」を「（ア）を選んだ・日付」のように書き換える） ③ （ア）ならその時点で「済」。（イ）（ウ）は、隠した後にエージェントが `/sign-in` と `/sign-up` を 375px で見直してから「済」 | 未決定（吉本さんの選択待ち） |
| 5 | A3 外枠 共有状態 | 上の帯の右側の**共有状態**が、PC の幅（1440px 前後）とスマホの幅（375px 前後）の両方で次のとおり出ること ── （ア）事業所を選んでいるとき: 緑の点・「事業所で共有中」・事業所の名前・切り替え（事業所の印と矢印）。**事業所の名前が2回並んでいない**こと（切り替えのボタンの中の名前は読み上げ用に隠す指定をしたが、Clerk の画面はログインしないと出ないので効いたかを見ていない）（イ）切り替えで個人のアカウントを選んだとき: 黄色の点・「自分の登録分のみ」と、帯のすぐ下の黄色い注意「ほかの職員が登録した利用者の名前は置き換わりません。複数人で使うときは、右上の事業所の切り替えから選んでください。」。スマホで帯からはみ出していないこと（ウ）（イ）のまま「つくる」で送る前の確認画面（赤い言葉がある文）を出して下へ動かしても、「前へ／次へ」の帯が黄色い注意の裏に隠れないこと（エ）**スマホの幅で、切り替えを押すと開く一覧の行の文字が読める**こと ──「個人のアカウント」の行（個人を選んでいるときは一覧の上の行、事業所を選んでいるときは一覧の中の行）と事業所の行に、印と矢印だけでなく名前の文字が出ていること。帯の中のボタンでは、スマホのとき個人のアカウントの名前を読み上げ用に隠している。最初の指定は Clerk が一覧の行にも使う名前に書いていたので、一覧の行の文字まで消していたおそれがあり（A3 の検証）、ボタンの中だけに効く指定に直した。部品だけを並べた確認用のページでは、ボタンの中だけ隠れて一覧の行は出ることを計測済み（下の「確かめたこと」）だが、本物の Clerk の一覧はログインしないと出ない | ① ログインして `/clients` を開き、右上の表示を見る ② 右上の切り替えで個人のアカウントを選び、黄色い注意を見る（「つくる」で確認画面まで進めて下へ動かす）。終わったら事業所を選び直す ③ ブラウザの幅をスマホくらいに狭めて ①② を繰り返す。そのとき右上の切り替えを押し、開いた一覧のどの行にも名前の文字が出ているかを見る | 未確認 |
| 6 | A3 外枠 左の帯と下のタブ | PC の幅: 左の帯に「利用者・つくる・点検・使い方」が並び、開いている画面の項目だけが濃く（アイコンが緑に）なること。`/rescue` はつくる、`/dashboard` は点検が光ること。下端に自分の丸いアイコンと、**メールの「@」より前**が文字で出ていること。スマホの幅: 同じ4項目が画面の下のタブに並び、どれも指で押せて、各画面の一番下の「CareNote — Powered by Claude API」がタブの裏に隠れないこと（iPhone では画面の下端の横線の上にタブが出ること） | ① ログインして左の帯の4項目を順に押す ② アドレス欄で `/rescue` と `/dashboard` を開き、光る項目を見る ③ スマホで開き、下のタブを順に押し、各画面の一番下まで動かす | 未確認 |
| 7 | A4 区画と旧画面の器 | （2026-09-24 追記: このうち利用者の一覧と詳細は A5 で作り替えた。その2画面は下の 9 で見る。一覧を一番下の人まで動かせることは 9 でも同じく見る）まだ作り替えていない7画面（利用者の一覧・利用者の詳細・つくる・点検〔評価〕・ダッシュボード・救済モード・使い方）が、**以前と同じ幅と中身**で出ること。PC の幅（1440px 前後）では**本文だけが動き**、左の帯・上の帯・一番下の「CareNote — Powered by Claude API」の1行は動かないこと。**利用者が10人より多いとき、「利用者」の一覧を一番下の人まで動かせること**と、ダッシュボードの評価履歴を一番下の行まで動かせること（2026-09-24: 行が本文の高さを超えると一覧と履歴が切れて下の行へ行けない不具合があり、直した。エージェントは仮のデータ〔40人・40件〕で計測済みだが、本物の中身はログインしないと見られない）。「つくる」で送る前の確認画面（赤い言葉がある文）まで進めて下へ動かすと、「前へ／次へ」の帯が本文の上端のすぐ下に止まり、隠れないこと。白い脇のボタン（「録音を始める」など）が小さく（高さ 34px・文字 12.5px）なったが、読めて押せること。スマホの幅（375px 前後）では今までどおり画面全体が動くこと | ① ログインして左の帯の4項目と `/rescue`・`/dashboard`・利用者の行を開き、以前と同じ中身か見る ② PC の幅で各画面を下まで動かし、左の帯・上の帯・一番下の1行が動かないことを見る。「利用者」は**利用者が10人より多い状態で開き、一番下の人まで動かす**（10人以下なら、その人数を状態の欄に書き添える）。ダッシュボードも評価履歴の一番下の行まで動かす ③ 「つくる」で確認画面まで進めて下へ動かし、「前へ／次へ」の帯を見る（スマホの幅でも同じ） | 未確認 |
| 8 | A4 ログイン直後の行き先（**本番の設定**） | **吉本さんにしてほしいこと**。ホームを利用者にした（`/` → `/clients`）。ログイン・新規登録の直後に開く画面は Vercel の環境変数で決まるので、本番の値を変えないと、ログインのあと以前どおり `/dashboard`（点検の履歴）が開く。redesign/a を main へ出すのと同じ時に、Vercel の Production の `NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL` と `NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL` を `/clients` に変える（エージェントは本番の設定を触らない） | ① Vercel の carenote-ai の Settings → Environment Variables を開く ② 上の2つの値を `/clients` に変えて保存し、Redeploy する ③ ログアウトしてからログインし、「利用者」の画面が開くか見る | 未確認（吉本さんの設定待ち） |
| 9 | A5 利用者の作業台 | PC の幅（1440px 前後）: `/clients` を開くと、左に利用者の表（記号・属性・登録日）、右に「左の一覧から利用者を選ぶと、書類と関係者名簿がここに出ます」とだけ出て、**誰も選ばれておらず、関係者の名前がどこにも出ていない**こと。上の帯の左に「利用者」と人数、「記号・属性で探す」、白い「新しい利用者」があること。行の記号（A様など）を押すと、その行の地が薄い緑になり、右の欄にその方の画面（関係者名簿・書類 ── 中身の見た目はまだ以前のまま）が出て、上の帯が「利用者 / A様」になること。別の行を押すと、表は動かず右だけが入れ替わること。利用者が10人より多いとき、表を一番下の人まで動かせて、動かしても表の見出し（記号・属性・登録日）が上に残ること。探す欄に「独居」などを入れると表が絞られること。「新しい利用者」を押すと右に登録の欄が出て（ボタンは押した色になる）、登録すると表の先頭に新しい記号の行が出て、その方の画面が開くこと。**登録した利用者は画面から消せない**（消す仕組みが無い）ので、登録は試験用の事業所か個人のアカウントでだけ試す。スマホの幅（375px 前後）: 上の帯に探す欄と「＋」が収まって、はみ出していないこと（見出しと人数は隠している。スマホ用の形は後の段 M1 で作るので、選んだ方の画面や登録の欄が表の下に出るのは今は正しい） | ① ログインして `/clients` を開き、右側に案内だけが出て名前が出ていないのを見る ② 行の記号を押し、行の色・右側・上の帯を見る。別の行も押す。探す欄に「独居」などを入れて絞られるか見る ③ ブラウザの幅をスマホくらいに狭めて、上の帯がはみ出していないか見る（登録は試験用の事業所か個人のアカウントでだけ試す） | 未確認 |
| 10 | A5 利用者の一覧の約束の1行（**決めること**） | **吉本さんに選んでほしいこと**。以前の利用者の一覧には、見出しの下に「利用者ごとに書類が貯まります（氏名は記号で表示）」という説明がありました（氏名を記号で出すという約束の文）。A5 で見出しを上の帯へ移したときに、この文が黙って消えていた（A5 の検証の指摘）ので、**表のすぐ上に小さな灰色の1行として戻した**。A案のアートボードにはこの1行が無いので、次の2つから選ぶ ── **（ア）残す**: 今の形。何もしなくてよい。**（イ）消す**: アートボードどおり。そのときも、利用者がまだいないときの文（「登録した氏名は暗号化して保存し、画面では A様 のような記号で表示します。」）と、「新しい利用者」の登録の欄の添え書き（「氏名は暗号化して保存し、画面では記号で表示します」）には同じ約束が残る。利用者がいる一覧を開いている間は、画面のどこにもこの約束が書かれていない状態になる | ① ログインして `/clients` を開き、表のすぐ上の1行を見る ② 残すか消すかを決め、（ア）（イ）をエージェントに伝える（この行の「状態」を「（ア）を選んだ・日付」のように書き換える） ③ （ア）ならその時点で「済」。（イ）なら、エージェントが1行とその検査（`components/clients/ClientTable.test.tsx`）を外してから「済」 | 未決定（吉本さんの選択待ち） |
| 11 | A6 利用者の区画（書類） | PC の幅（1440px 前後）で、書類を保存したことのある**試験用の**利用者の行の記号を押し、右の区画が上から「B様（仮名）」（大きな等幅の文字）・緑の枠の「仮名表示中」・属性の1行・緑の「つくる」、その下に「書類」（5種類が1行ずつ。ある種類は「下書き／承認済み」の札・日付・「開く」、無い種類は「まだありません」と「つくる」）・「一式まとめて」・「関係者名簿」・文字起こし、の順に並ぶこと。同じ種類を2回以上保存した方では「以前の版（n）」を押すと古い版が並び、それぞれ「開く」で開けること。「開く」を押すと右の区画が少し広がり（640px）、「書類の一覧へ戻る」・書類の名前・札・承認の操作（下書きなら緑の「承認する」と押せない「コピー」＋「承認後にコピーできます」）と中身が出ること。**緑のボタンが画面に1つだけ**であること。「つくる」「一式まとめて」を押すと、つくる／救済モードの画面が開くこと（利用者や書類の種類はまだ引き継がれない ── 後の段 C2・C9 で入れる）。ログインが要るので、エージェントは本物の画面を見ていない | ① ログインして `/clients` を開き、試験用の利用者の記号を押す ② 右の区画の並びを見て、書類の「開く」を押し、承認の操作と中身が出るか見る（「承認する」は試験用の書類でだけ押す） ③「書類の一覧へ戻る」で戻り、「以前の版」があれば開いてみる | 未確認 |

## 確かめたこと（ログインなしで開ける画面）

- 2026-09-24 A5 の検証の直し（**確認専用の静的なページ**〔本物の `app/globals.css` を丸ごと読み込み、外枠 → `.panes` → 一覧の区画〔約束の1行＋表 40人〕｜右の 440px の区画、という並びを HTML で書いたもの。React の部品は描いていない・IBM Plex は読み込んでいない〕・コミットしていない・確認後に削除）: 一番下の AN様にフォーカスを置き、本物のキー操作で Shift+Tab を1回ずつ押して、押すたびにリンクの位置と、リンクの真ん中の点に何があるか（elementFromPoint）を計測。**1440×900・直す前**（`.pane:has(> .client-table)` を 0px に戻した状態）: 区画の `--sticky-top` 0px・`scroll-padding-top` 8px。O様のリンクが区画の上端から 8〜52px に止まり、0〜40px を覆う見出しの行の裏に隠れた（真ん中の点は見出しのセル「記号」）── 検証の指摘と同じ。**1440×900・直した後**: 一覧の区画の `--sticky-top` 40px・`scroll-padding-top` 48px（右の区画は 0px のまま）。AN様から A様まで押して、隠れたリンクは0。上へ動かすところで Y様が 48〜92px に止まり、見出しの行（0〜40px）の下に出た。`scrollIntoView({block:'nearest'})` でも K様が 48〜92px。約束の1行は高さ 44px・文字 12px・`#59616a`・下に 1px の線（#e6e9ec）。**375×812**: 横にはみ出す物は0。区画は動かず（文書が動く）、区画に `--sticky-top` は無い。文書の `scroll-padding-top` は 60px（上の帯の高さを測る部品が無いので控えの 52px＋8px）。Shift+Tab で上へ戻ると AC様が 60〜104px に止まり、隠れなかった（見出しの行 0〜40px は上の帯 0〜52px の裏にある）。**見ていないもの**: ログインした本物の `/clients`（上の表の 9・10）と、本物の部品で描いた画面（静的なページは CSS だけが本物）。

- 2026-09-24 A5 利用者の作業台（**本番用ビルド**〔`npm run build` → `next start`〕・ログインが要らない**確認専用の仮のページ**〔外枠の部品と本物の `ClientsLayout`・表・登録の欄・利用者の詳細を描き、`/api/clients…` だけを仮のデータ ── 利用者 40人 ── で返す。URL は仮のページのものなので、選んだ状態だけは `ClientsLayout` と同じ並びを仮のページに書いて描いた〕・コミットしていない・確認後に削除）: ログインしていない ＝ 注意の帯ありの状態。**1440×900**: 文書は動かない（900）。表の区画は幅 928px（x=72〜1000）、右の区画は幅 440px（x=1000）で左に 1px の線・地 `--pane`・名前「利用者の詳細」・中身は案内の1文だけ。表の見出しの行 40px（貼りつく）、行 52px・区切り 1px（#e6e9ec）、記号のリンクは高さ 44px で等幅、登録日は等幅の「2026/09/01」。表の区画だけが動き（動かせる 2120／見える 781）、一番下の AN様は y=819〜871（区画の下端 871 の内側）で、その位置を指すと AN様のリンクに当たる。動かしても見出しの行は y=90 に残り、右の区画は動かない。上の帯: 見出し 15px・人数「40」（等幅）・探す欄 280×34px・「新しい利用者」34px（帯の右寄り）。探す欄に入ると欄全体に緑の輪。探す欄に「Ｂ様」（全角）→ B様だけ、「要介護9」→「「要介護9」に当てはまる利用者はいません。」と「探す言葉を消す」。`?new=1`: 右の区画に頭の帯「新しい利用者」＋「やめる」（/clients）、欄の id 5つ・名前5つ、年齢と性別は横に2列（各 186px）、緑は「利用者を作成」（44px）の1つだけ。**「新しい利用者」の押した色が出ていなかった**（globals.css の層 components に書いた地の色が、btnSecondary の Tailwind の白い地〔層 utilities〕に負けていた）→ 部品の側の `aria-[current=page]:bg-[var(--active)]` に直し、作り直したビルドで `#dde2e6` を確認。読めなかったとき: 知らせ（レンガ色）に「利用者一覧を読めませんでした。少し待ってから…」と「一覧をもう一度読む」、表も「まだ利用者がいません」も人数も出ない。0人のとき: 「まだ利用者がいません」と暗号化・記号の約束の文（「A様」は等幅）・人数「0」。選んだ状態（B様）: 行の地 #e7efeb、リンクに aria-current、道しるべ「利用者/B様」、見出し h1 は右の区画の「B様（仮名）」の1つだけ、以前の詳細の中身が 440px に横はみ出しなしで収まる。**375×812**: 横にはみ出す物は0。案内だけの右の区画は出ない（display none）。上の帯は、本物の `/clients` で右に出る「この画面の使い方」（44px）の代わりの箱を足した状態で、探す欄 119×44px（文字 16px ── iPhone が拡大しない大きさ）、「＋」44×44px、見出しは読み上げ用（1px）・人数は出さない。直す前は見出しと人数が並び、探す欄は約35px になる計算だった（使い方の印が無い仮のページで 83px）。登録の欄・選んだ方の画面は表（40人・高さ 2186px）の下に出る（スマホ用の形は M1）。**見ていないもの**: ログインした本物の中身（上の表の 9）、本物の URL での行の押し方と画面の移り方（仮のページからは本物の `/clients/{id}` へ移るとログイン画面になる）、スマホで行を押したときに右の区画の中身まで画面が動くか（Next.js の既定の動きで動くはず・未確認）。

- 2026-09-24 A4 の直し（動く器の直下の物を縮ませない）の計測（**本番用ビルド**〔`npm run build` → `next start`〕・ログインが要らない**確認専用の仮のページ**〔外枠の layout と本物の7画面を描き、`/api/…` への問い合わせだけを仮のデータ ── 利用者 40人・評価履歴 40件・書類 40件 ── で返す〕・コミットしていない・確認後に削除）: ログインしていない ＝ 注意の帯ありの状態。**比べ方**: 同じビルドの画面に「直下の物を以前どおり縮ませる」指定（`flex-shrink: 1`）を差し込むと直す前の形になり、外すと直した形に戻る。**利用者 1440×900**: 直した形は一覧の枠 3887px（40行）、本文の見える高さ 781・動かせる高さ 4057、一番下の AN様まで動かすと y=734〜831 で本文の下端（y=871）の内側、その位置を指すと AN様の行に当たる（押せる）。文書は動かない（900）。直す前の形は枠が 612px に縮み、動かせる高さ＝見える高さ（781＝781）、AN様は y=4010 のまま届かない（検証の指摘と同じ）。**利用者 1024×768**: 動かせる 4057／見える 649、AN様 y=602〜699（本文の下端 739 の内側）。**ダッシュボード（推移のグラフあり）1024×768**: 履歴の枠 2357px、動かせる 2936／見える 649、最後の行 y=642〜698。直す前の形は枠が 70px まで縮み、649＝649 で最後の行（y=2929）に届かない。**1440×900**: 動かせる 2936／見える 781、最後の行 y=774〜830。**空の状態**（背の低い画面 1024×480）: ダッシュボードの「まだ評価履歴がありません」の枠は、以前の `flex-1` のままだと 260px に縮み、「最初の評価を開始する」（y=420〜464）が枠の外で切れて見えなかった。`grow` に替えた後は枠 378px に中身が全部入り、ボタンは y=302〜346 で本文の中。1440×900 では空の枠が本文の下まで埋まる（下端との間は `::after` の 40px だけ・利用者の空の枠も同じ）。**残りの5画面**（1440×900）: 利用者の詳細（書類 40件）動かせる 2868／見える 781、救済モード 2161／781、使い方 22411／819、つくる 818／781、評価 781／781（中身が本文に収まる）。どれも本文の直下の物は縮まず（flex-shrink 0）、overflow-hidden の物で中身が切れている物は0、一番下まで動かすと最後の物の下端は本文の下端より上。**区画（`.pane`）**: 頭の帯の下に overflow-hidden の枠（40行×60px）を仮に置くと、直した形は区画が動き（2449／781）最後の行の下端が区画の下端（y=871）に届く。直す前の形は枠が 733px に縮み、781＝781 で届かない。**375×812**: 利用者とダッシュボード（各40件）は今までどおり文書が動き（高さ 4296・3573・横 375px ちょうど）、最後の行の下端 y=663 は下のタブの上端 y=756 より上。**分かったこと**: 前日の計測はデータが空で、行が本文を超える形を一度も見ていなかった（下の訂正）。**見ていないもの**: ログインした本物の中身（上の表の 7）。

- 2026-09-23 A4 区画と旧画面の器（**本番用ビルド**〔`npm run build` → `next start`〕・ログインが要らない**確認専用の仮のページ**で外枠の layout と本物の7画面・区画の部品を描いた・コミットしていない・確認後に削除）: ログインしていない ＝「自分の登録分のみ」＋黄色い注意の状態で計算後の値を計測。**1440×900**: 文書は動かない（scrollHeight 900＝画面の高さ・横にもはみ出さない）、外枠 900px＝上の帯＋注意 90＋本文 781＋一番下の1行 29、左の帯 900px。区画の部品: 640px と残り 728px が 1px の線で並び、左を 900px 動かしても右は 0 のまま、両方の頭の帯（48px）は上に残る、右の区画の中の「前へ／次へ」は頭の帯の 8px 下（y=146 ＝ 90＋48＋8）に止まる。頭の帯の裏に置いたボタンへ最小限だけ動かす（`scrollIntoView` の nearest ── Tab で移るのと同じ動き）と帯の 8px 下（y=146）に出る（scroll-padding を 0 にすると y=100 で帯の裏のまま）。小見出し 12px・太字・字間 0.72px、文字だけの操作 12.5px・高さ 28px、白い脇のボタン 34px。旧画面の器: 7画面とも中身の左端 x=296・幅 905〜920px（旧: 中身 920px を真ん中に）、本文の中だけが動く（救済モード 2172px を下まで動かせた。**訂正 2026-09-24: 「7画面とも動く」は誤り**。この計測はログインなし＝API が何も返さない状態で、利用者の一覧と点検の履歴に行が1つも無かった。行が本文の高さを超えると、一覧と履歴の枠が本文の高さまで縮んで行を切り落とし、下の行へ行けなかった ── 直した後の計測は上の 2026-09-24 の記録）、使い方の `#ch3` を直接開く・目次から `#ch5` へ飛ぶ、どちらも本文の上端の 8px 下（y=98）。「前へ／次へ」は本文の上端の 8px 下（y=98）に止まる ── **最初は 48px 下に止まっていた**（動く器の padding-top 40px のぶんブラウザが基準を下げる）ので、上下の 40px を空の箱（::before/::after）に替えて直した。**375×812**: 画面全体が動き（横 375px ちょうど）、器の余白は 16/16/32px、本文の終わり（一番下の1行の下端 y=756）は下のタブの上端（y=756）で止まる、区画は縦に積まれ 1px の線、頭の帯は貼りつかない、文字だけの操作と白い脇のボタンは 44px。「前へ／次へ」は帯の測った高さ＋8px（帯＋注意 133px → y=141）。**800×420**: 文書は動かない、左の帯は 420px に収まる、旧画面の器の中身は左端 112px・幅 633px（旧と同じ）。**分かったこと**: 描画が止まった枠では `animate-fadeIn` が最初の位置（8px 下）で止まったままになり、測った位置が 8px ずれる（計測の前にアニメーションを終わらせた）。**見ていないもの**: ログインした本物の中身（上の表の 7）、事業所を選んでいる状態（5）。

- 2026-09-23 A3 外枠（開発サーバー・1440×900 と 375×812）: ログインが要らない**確認専用の仮のページ**（外枠の layout をそのまま描く・コミットしていない・確認後に削除）で、ログインしていない状態 ＝ **「自分の登録分のみ」＋黄色い注意**を計算後の値で計測。1440px: 左の帯 幅 72px・画面の高さいっぱい・上に貼りつく、4項目それぞれ 60×58px、上の帯 52px、黄色い注意 38px、測った高さ `--shell-head-h` = 90px（帯 52＋注意 38）。900px 下へ動かしても上の帯は上端に残り、「前へ／次へ」の帯（`.presend-nav`）は上の帯の 8px 下（y=98）に止まる。横にはみ出さない。375px: 左の帯は画面の下のタブ（4項目それぞれ 94×55px・上の線と合わせてタブの高さ 56px・CN とアカウントの欄は出さない）、黄色い注意は3行に折り返して 81px（帯と合わせて 133px）、`--shell-head-h` = 133px、「前へ／次へ」は y=141（帯の 8px 下）、使い方の章の見出し（`#ch2` へ飛ぶ）は y=149（帯の 16px 下）、本文の終わり（「Powered by Claude API」の欄の下端 y=756）はタブの上端（y=756）でちょうど止まり、裏に潜らない、横にはみ出さない（幅 375px ちょうど）。**分かったこと**: 画面が描かれていない（隠れた）タブでは ResizeObserver が動かず、`--shell-head-h` は古い値のまま（52px）になる。タブが見えた時点で書き直されることを確認した（133px）。**見ていないもの**: 事業所を選んでいる状態（緑）と、Clerk の切り替えのボタン・アカウントのボタンの見た目（ログインしないと Clerk が描かない）→ 上の表の 5・6。

- 2026-09-23 A3 外枠の検証の追補（本番用ビルドの CSS をそのまま読む**確認専用の静的なページ**・外枠と同じ部品の並び・コミットしていない）: **フォーカスが帯やタブの裏に隠れない**こと（WCAG 2.2 AA 2.4.11）を計測。375×812（注意の帯あり・帯＋注意 112px ── 確認用ページは IBM Plex を読み込まないので、上の 133px とは折り返しが違う）: 下のタブ（上端 y=756）の裏のボタン（y=760〜804）に `focus()` → y=412〜456 へ動く。Tab キーで同じ位置のボタンへ移っても y=412 へ動く。帯の裏のボタン（y=60・帯の下端 112）に `focus()` → y=412 へ動く。比べるために scroll-padding だけを 0 に戻すと、どちらも動かず隠れたまま（y=760 / y=60）。使い方の章（`#ch2`）へ飛ぶと、見出しは y=120（帯の 8px 下 ── 旧 `.shell-anchor` は 16px 下だった）。html の計算後の値は scroll-padding-top 120px（112＋8）・scroll-padding-bottom 64px（56＋8。safe-area は 0 の画面）、横にはみ出さない。1440×900: 下は空けない（scroll-padding-bottom auto）、帯の裏のボタン（y=20）に `focus()` → y=488 へ動く。**切り替えのボタンの中の文字**: 375px では `max-md:[&_.cl-userPreviewTextContainer]:sr-only` でボタンの中の個人のアカウントの名前だけが読み上げ用（幅 1px・position absolute）になり、ボタンの外に置いた一覧の行（Clerk と同じクラス名 `cl-userPreviewTextContainer cl-userPreviewTextContainer__personalWorkspace`）の文字は出たまま（position static）。1440px ではボタンの中の名前も出る（幅 82px）。**見ていないもの**: 本物の Clerk の一覧（ログインしないと出ない）→ 上の表の 5（エ）。

- 2026-09-23 `/sign-in`・`/sign-up`（開発サーバー・1440px と 375px）: Clerk のスタイルが `clerk` 層に入り、`lib/clerkAppearance.ts` の
  クラスが効いていることを計算後の値で確認 ── 外枠 1px `#dfe3e7`・影なし・角丸 10px、ボタンの地 `#0e5c46`、見出し 17px 太字、
  ラベル 12.5px 太字。Clerk 自身の余白（枠の中 32px 40px）は `@layer base` の全要素リセットに負けていない。375px で横にはみ出さない。
  文字はどれも読める。本番用ビルド（`npm run build`）の CSS でも、層の並び（theme → base → clerk → components → utilities）と
  これらのクラスが生成されていることを確認した。本番用サーバーでの描画そのものは見ていない（上の1の手順①でログイン画面が見える）。
- 2026-09-23 `/sign-in`・`/sign-up`（開発サーバー・375px と 1440px）: **押す・入力する部品の大きさ**を計算後の値で計測。
  直す前（375px）は入力欄・主ボタン・Google のボタンが高さ 31.4px、パスワードを見せるボタンが 40×24px、下の切り替えリンク
  （Sign in / Sign up）が高さ 19.4px だった。`lib/clerkAppearance.ts` に `min-h-11` などを付けた後は、どれも高さ 44px
  （パスワードを見せるボタンは 44×44px、切り替えリンクは 44.9×44px と 49.7×44px）。375px で横にはみ出さない。
  パスワードの入力欄は右を 48px 空け、入力した文字の終わり（右端から 48px）とボタンの左端が同じ位置で、文字がボタンの下に潜らない。
  切り替えリンクを 44px にしたとき、横の案内文（Don’t have an account?）が上に寄ってずれたので、並びの行を上下中央揃えにした
  （直した後、案内文とリンクの文字の中心の高さが一致することを計測）。
  **直せていないもの**: 下の「Secured by Clerk」の Clerk のロゴのリンク（48×14px）は、Clerk が見た目の指定の口
  （elements の名前）を出していないので、この設定では大きくできない。どうするかは上の表の **4** で吉本さんが決める
  （決まって「済」になるまで redesign/a を main へ出さない）。

*2026-09-23 新設（A1 の検証で「/clients の実画面確認が済んでいない」と指摘されたため。§2.7-A: 追跡せずに先送りしない）*

*2026-09-23 追記: 3 を追加（A1 の検証で「スマホで押す場所が 44px に足りない」と指摘され、1段目を直して計測した。2段目は追跡する）*

*2026-09-23 追記: 4 を追加（A1 の検証で「ロゴのリンクの件が文章の中にしか無く、表の『済』の関門から漏れていて、吉本さんの決定も記録されていない」と指摘されたため。§2.7-A）*

*2026-09-23 追記: 5・6 を追加し、2 の置き場所を書き換えた（A3 外枠 ── 共有状態を上の帯へ移し、左の帯と下のタブにした。ログインが要る見た目はエージェントが見られないため）*

*2026-09-23 追記: 5 に（エ）を追加し、フォーカスの計測を「確かめたこと」に足した（A3 の検証 ── 切り替えの一覧の行の文字を消していたおそれ・フォーカスが帯やタブの裏に隠れていた）*

*2026-09-23 追記: 7・8 を追加し、A4 の計測を「確かめたこと」に足した（区画と旧画面の器・ホームを利用者に ── ログインが要る見た目と、本番の Vercel の設定はエージェントが触れないため）*

*2026-09-24 追記: 7 に「利用者が10人より多い状態で一番下の人まで動かす」を足し、A4 の計測の「7画面とも動く」を訂正して、直した後の計測を足した（A4 の検証の blocker ── 行が本文の高さを超えると一覧と履歴が切れて下の行へ行けなかった。前日の計測はデータが空だった）*

*2026-09-24 追記: 10 を追加し、A5 の検証の直しの計測を「確かめたこと」に足した（検証の指摘 ── 表の見出しの行の裏にフォーカスが隠れていた・氏名を記号で表示する約束の文が黙って消えていた。戻したうえで、残すか消すかを吉本さんが決める）*
